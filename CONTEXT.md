# mycmux — 機能別用語集

## 用語 (「メール」機能 — 未返信メール監視)

**未返信 (pending)** — 自分が To/Cc に含まれるスレッドのうち、最後の社外発話より後に社内 (own_addresses + internal_domains) の発話がないもの。例外として、最終メッセージが社内発で自分に To 直宛のもの (社内からの依頼) も未返信に数える。
_使わない同義語_: ボール、返信義務、未対応

**社内対応済み** — 社外の最終発話の後に、自分以外の社内メンバーが返信して未返信が解消された状態 (edu.math BCC の社内ルールにより監視ボックス内で機械検出できる)。件数に数えない。
_使わない同義語_: 代理返信、他者対応

**自分のアドレス (own_addresses)** — 判定で「自分」とみなすメールアドレスの集合。alias 送信 (e.miyazaki 等) を含む。
_使わない同義語_: 監視アドレス、自アカウント

**監視アカウント (watched account)** — Google 認証で紐づけた Google アカウントのうち、走査対象として選択したもの。複数選択可で、同一スレッドが複数アカウントに現れた場合は 1 件に名寄せする。
_使わない同義語_: 監視ボックス、対象メールボックス、連携アカウント (=認証済みだが未選択のものを含む広い語なので使わない)

**消し込み** — 未返信の件数から件を減らすユーザー処置の総称 (返信不要・後で・永久無視)。Gmail 側には一切反映しない。
_使わない同義語_: 既読化、アーカイブ、クローズ

**論理会話** — 未返信判定・件数・消し込み・スヌーズ・下書きの最小単位。基本は Gmail thread で、複数の監視アカウントに同じ会話が現れた場合は Message-ID と返信連鎖で名寄せして1つとして扱う。
_使わない同義語_: スレッド (アカウント内の Gmail thread を指す語として区別する)、案件

**会話終端** — 「ありがとうございました」等、返信を必要とせずに終わる社外メッセージ。決定的判定では未返信になるため、LLM の後段判定で優先度を下げて弱表示に落とす (ADR 0007)。
_使わない同義語_: クローズ、完了メール

**ディグ** — メール本文の内容を探す行為。未返信カード列内の検索ボックス (未返信分のみ) と、チャットへの依頼 (全メール対象) の2導線。
_使わない同義語_: 全文検索、メール検索UI

## 用語 (Web ペイン — ChatGPT Pro 連携・ADR 0008)

**Web ペイン (web tab)** — 外部 Web サービスを mycmux 内の native webview (WebView2 child webview) で表示するタブ。`PaneTab.type = "web"`。ターミナルタブと同格で、分割・移動・close-tab・再起動復元の対象。
_使わない同義語_: ブラウザペイン (=既存 `BrowserPane` のローカル成果物プレビューを指すので使わない)、ブラウザタブ、GPT パネル

**プリセット (preset)** — Web ペインの起動定義。URL・専用プロファイル・許可ホスト・CDP ポート・ランチャー項目の組。v1 は `chatgpt` の 1 件。
_使わない同義語_: ブックマーク、サイト設定、テンプレート

**ChatGPT ペイン** — プリセット `chatgpt` で開いた Web ペイン。人間が ChatGPT Web を操作する場所であり、Phase 2 以降は oracle の attach 先でもある。
_使わない同義語_: GPT ペイン、oracle ペイン、GPT タブ

**プロファイル (profile)** — プリセットごとに分離した WebView2 の `data_directory`。ログイン cookie と ChatGPT 側の設定 (Pro effort の選択状態) を保持する。メイン UI のプロファイルとも他プリセットとも共有しない。
_使わない同義語_: セッション (=会話やターミナルセッションと衝突する)、ユーザーデータ

**エンジン (engine)** — oracle の CDP 自動化が attach する Chromium の実体。現行は画面外 Chrome (9222)、Phase 2 合格後は ChatGPT ペインの WebView2。
_使わない同義語_: バックエンド、ドライバ、ブラウザ本体

**push (載せる)** — mycmux 側から ChatGPT ペインの composer にテキストとファイルを載せる操作。既定は送信しない (人が送るか `--send` 明示)。
_使わない同義語_: 送信、投げる、注入 (=実装手段の語であり操作名にしない)

**consult (投げて待つ)** — エージェントが oracle 経由で ChatGPT に問いを送り、完了を待って回答テキストを受け取る往復。push と違い送信まで自動。
_使わない同義語_: 相談、クエリ、oracle 呼び出し

**引き継ぎ書 (brief)** — エージェントがセッションの文脈を GPT に渡すために書く短い markdown (`brief.md`)。論点・経緯・問い・制約・添付一覧を含む。生 transcript は渡さない。
_使わない同義語_: サマリー、要約、コンテキストダンプ、transcript

**handoff フォルダ** — 引き継ぎ書・同梱ファイル・ChatGPT からのダウンロードを置く場所。`~/.mycmux/handoff/<preset>/…`。
_使わない同義語_: 受け渡しフォルダ、exchange、Downloads

**段階移行 (Phase 1 / Phase 2)** — Phase 1 = 人間用の ChatGPT ペインと push を作る。Phase 2 = oracle を ChatGPT ペインの WebView2 に attach する spike を通し、合格なら Chrome 常駐を廃止する。
_使わない同義語_: v1/v2 (=機能の版数として別に使う)、統合、移植
