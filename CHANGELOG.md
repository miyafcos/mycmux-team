# 変更履歴 (mycmux personal)

このファイルは personal 版 (`master`) の変更履歴です。チーム配布版 `mycmux-lite` は lite worktree 側の `CHANGELOG.md` で管理します。

---

## [0.67.0] - 2026-09-08

左上のベルに溜まった通知を、対応しないまま片づけられるようにした版。

- New: 通知パネルに **「すべて既読にする」** (ヘッダー) と、**全行の × ボタン** を追加。要対応 (質問・承認) の行にはこれまで一括操作も個別操作もなく、エージェントが次に進むまで消えなかった
- Fix: どちらの操作も、カウンタをゼロにするだけでなく **既読の印を付ける**。質問はセッションの生きた状態に居座るので、印を付けないと同じ行が未読として戻ってくる。印はペインのタブピルが読むものと同じで、ベルとタブの見え方が揃う
- New: 設定「通知とレイアウト」に **「左上のベル」** セクション。質問 / 承認待ち / 作業完了 / その他の未読を種類ごとにオンオフでき、オフにした種類は一覧からもバッジの数字からも消える
- Change: マスターの「通知」をオフにすると、右下トーストだけでなく **ベルも静かになる** (これまでは要対応だけベルに出続けた)。設定画面にその旨を明記
- Change: 未読セクションにあった「この区分を既読に」はヘッダーの「すべて既読にする」に統合
- Fix: 識別子を持たない attention は要対応に出さない。既読の印を付けられず、消す手段のない行になっていた (ペインのタブピルとダッシュボードは元々そういう行を扱わない)

---
## [0.66.0] - 2026-09-08

AI エージェントが Web サービスを Chrome なしで、mycmux の裏タブだけで操作できるようにした版 (Windows 資産を先行公開。macOS 資産は Mac 側のビルド後に追加)。

- New: **Web 自動操作層**。`web.navigate` / `web.wait` / `web.eval` / `web.snapshot` (可視要素を role・name・ref つきで返す) / `web.find` / `web.click` / `web.type` / `web.key` / `web.scroll` / `web.upload` (DataTransfer 経由の添付) / `web.screenshot` / `web.downloads` / `web.dialogs` の 13 コマンドを追加し、CLI `mycmux_agent_cli.py web-*` から使える。ref は次の snapshot かページ遷移まで有効
- New: **AI 用の汎用プリセット `browser`**。任意の HTTPS と `http://localhost` / `http://127.0.0.1` を開ける専用プロファイル (`web-profiles/ai`)。ランチャー (`Browser (Web)`)・New Workspace のカタログ・地球アイコンにも登録。`alert` / `confirm` / `prompt` は browser だけ記録して自動応答
- New (Windows): **アプリ内 CDP** による信頼入力とスクリーンショット。`--trusted` でクリック・キー・insertText・`<input type=file>` への実ファイル設定 (isTrusted=true)、`web-screenshot` は CSS ピクセル寸法の PNG。デバッグポートは開けない。3 コマンドは primary webview 以外から呼べない
- New: ダウンロードを `~/.mycmux/handoff/web/<preset>/downloads/` に受けて `web.downloads` で一覧 (同名は `-2`, `-3`)
- Safety: 1 ソケットコマンドの期限は受信から 25 秒 (キュー待ち込み)、ネイティブ層は 20 秒・CDP 1 呼び出し 4 秒、同一タブのコマンドは直列化。`web.eval` の script 256 KB・添付 25 MB・`clickCount` 1〜3・`snapshot` の `maxBytes` 4096 以上。`web.eval` は strict CSP のページでも動く 2 段構え
- Fix: CDP が int32 を超える数値を小数 (`2147483648.0`) で返すため、世代番号が 2^31 以上のとき trusted 操作が拒否されていた (実機 E2E で検出)
- Test: fixture `tests/fixtures/web-automation/index.html` と `scripts/verify_web_automation.py` (テスト機の裏タブで 24 手順を検証。Windows は trusted と screenshot を含む)
- Docs: `docs/agent-integration.md` の「Web 操作」節 (基本ループと全コマンド表)、`docs/plans/2026-09-08-web-automation-layer.md` (設計・境界・既知の制約)

---
## [0.65.0] - 2026-09-07

Web ペインを裏タブでもエージェントから使えるようにし、mycmux 前提の Claude Code スキルを同梱・導入できるようにした版 (Windows と macOS の資産をそろえて配信)。

- New: **Web ペインの裏タブ操作**。`web.open` に `url` と `background` (フォーカスを奪わずに開く)、`web.read` (ペインの会話を JSON で読む)、`web.close` を追加し、`web.list` に background / active を載せた。裏タブの webview は隠さず画面外に置くので、ChatGPT / Gemini / Grok の逐次表示が裏でも止まらない
- Fix: Web ペインの送信ボタンを Gemini の日本語ラベル「プロンプトを送信」でも見つける (出現待ちつき)。Grok の入力欄 (TipTap) への貼り付けと送信
- New: **Claude Code スキルパック**。`skills/claude/` に session-dispatch・mycmux-bridge・oracmux と agent CLI を同梱し、`python scripts/install_claude_skills.py install` で `~/.claude/skills/` へ導入できる。manifest (LF 正規化 SHA-256) で照合し、ローカル改変は黙って潰さない
- New: 設定 → AI の「Claude Code スキル」から同梱の 3 スキルと agent CLI を導入・更新でき、ローカル改変は旧フォルダを退避して置き換える。前提 (Claude Code / Python 3.10 以上) の有無もカードに表示する
- Docs: README「Claude Code スキルを入れる」節、`docs/agent-integration.md` の配布物・展開チェックリスト、`docs/features/implemented/socket-api-and-automation.md` の `web.*` 追記

---

## [0.64.1] - 2026-09-07

Claude Code の hook 登録が起動のたびに増えていく不具合を止めた版 (Windows 資産を先行公開。macOS 資産と updater feed は Mac 側のビルド後に追加)。

- Fix: **mycmux が Claude Code / Codex の settings に登録する hook を、`mycmux_managed` の印でなく command の形 (`mycmux_hook.py` + `--provider`) で見分ける**。Claude Code が settings.json を書き戻すと印が落ち、再起動のたびに同じ登録が積み上がっていた (9/5 時点で 25 件・9/7 に 5 → 10 の再発を実測)。回帰テスト 5 件
- New: 通知ベルを「回答が要る席」と「未読の到着」の 2 区分にし、ベルから質問・承認に答えられる (試用中。ask-question の input revision 再バインド・停止理由の表示・未送信回答の再試行を含む)
- Fix: spawn した子タブに親の一時起動環境 (`MYCMUX_HANDOFF*`) を渡さない。背景 workspace へ spawn した pane の PTY が起動しなかった件を修正
- Fix: CLI `send --expect-attention-id null|none` を「attention なし」として扱う。headless のスクロールバックを pane の実サイズで再生し、質問カードの読み取り失敗 (画面の質問を読めなかったため送っていません) を解消
- Docs: docs/・README・CHANGELOG・ADR 0009 を v0.64.0 の実装に整合

---

## [0.64.0] - 2026-09-06

ランチャーの「開発」「案件」が、誰の環境でも設定から育てられるようになった版。中身を入れる手段が私設スクリプトと手編集しか無かったところを、アプリ内蔵の台帳・候補・自動登録に置き換えた。Windows と macOS で同じ動き。

- New: **設定 → ランチャー に「フォルダ」「候補」「自動登録」「詳細」のブロック** — 「フォルダを選んで登録…」で登録、表示名はクリックで編集、手動の行は ↑↓ で並べ替え、自動の行は「固定」で手動へ昇格、「無視」で以後出さない。セクション名 (開発 / 案件) も変えられる
  - 候補 = ルールが見つけたフォルダ・エージェントを動かしたフォルダ (ailog)・最近選んだフォルダのうち未登録のもの。「開発に登録」「案件に登録」「無視」で処理する
  - 自動登録のルールは 4 種 — git リポジトリ (親フォルダ直下の最近活動したリポジトリ) / ファイル更新 (根の下を代表階層で束ねる) / 動かした場所 / セッションで触れた (会話本文に出たパス)。ルールごとに「候補にする」「自動で登録する」を選ぶ。走査は起動 15 秒後と 3 時間おき、または「今すぐ走査」。1 ルール 30 秒・50,000 フォルダで打ち切り、シンボリックリンクとジャンクションは追わない (Dropbox / OneDrive のクラウド用ファイルは通常どおり走査する)
  - 新規インストールの既定は「git リポジトリ (ホーム直下)」と「動かした場所」の 2 本を候補モードで。勝手には増えない
- New: **正本が `~/.mycmux/launch-dirs.json` に移った**。`launch-roots.txt` は初回に自動で取り込み、以後はアプリが書き出す派生ファイル (bash 経路の `d` / `a` キー用)。外部で書き換えられた行は次の読み込みで取り込む。私設スクリプトが書く AUTO ブロックの目印も保つので、移行期間中も毎朝の更新は止まらない
- Change: ランチャーペインの開発・案件は台帳を描く (手動 → 自動の順・印は自動行だけ)。空のセクションは「登録なし — 設定で登録 (候補 N 件)」から設定タブへ飛べる。フォルダを選ぶと「最近使った」(MRU) に記録される
- Change: 同梱 `launcher.sh` から私設スクリプト (`update_launch_anken.py`) の呼び出しと `事務関係/` 決め打ちの表示短縮を外し、派生ファイルの `# short-root:` 行で短縮する
- Fix: JSON 台帳の保存は内容が変わったときだけ (読むたびに `.bak` が回転していた)。破損時は `.bak` → `launch-roots.txt` → 既定の順で復旧する
- Fix (mac / win 両対応レビュー 19 件のうち 17 件を反映): **macOS で `~/Dropbox` (実体は `~/Library/CloudStorage/Dropbox` へのシンボリックリンク) や `/tmp` 配下を根にしたルールが「not a directory or linked root」で全滅していた** — 明示した根と実在確認はリンクを辿り、走査で見つけた子だけリンクに降りない。自動ルール同士の照合を 1 回にまとめ、別ルールの行が次の走査で一度消える・「セッションで触れた」が「ファイル更新」に勝てない問題を直した。外付けが外れているなど親フォルダが無いルールは「0 件で成功」でなくエラー扱い (既存の行は残る)。会話本文の絶対パスは根のフルパスが一致するときだけ数える (別の場所の同名フォルダを拾わない。親フォルダ名の空白や、パスの前後の文章があっても拾う)
  - `launch-roots.txt` の外部編集は mtime でなく内容で検出し、書き出し直前にもう一度確かめる。UTF-8 で読めない txt は `.unreadable-<秒>` へ退避してから書き出す。初回取り込みは並び順に関係なく手動行が勝つ。相対パス・改行入りのパスは受け付けない。外部取り込みが起きた読み込みは他の窓にも通知する
  - 派生 txt: `#` 始まりのラベルは `＃` に、開発の `案件:` 始まりは `案件：` に書き出す (bash がコメント・案件扱いしない)。bash の読み手は CRLF と最終行の改行なしを許容し、案件の判定を `案件:` (コロン形) に揃えた。MRU の一時ファイル名を書き手ごとに分けた
  - 新規の git ルールに既定の除外 (`_` `.` `~$` / `AppData` `Dropbox` `OneDrive` / `backup`) が入る。MSYS の `/c/` 書き換えは Windows だけ。3 時間おきの走査が 1 秒差で 6 時間後になる刻み問題。中間省略がサロゲートペアを割る問題
  - 保留 = macOS の File Provider (オンデマンド) フォルダの列挙が 30 秒の締切で止められない件 (走査は裏で動くので UI は止まらない)
- 設計と実装記録 = `docs/plans/2026-09-05-launcher-dirs-settings-requirements.md` (§14 Phase 1 / §15 Phase 2 / §16 Phase 3)。テスト: Rust +62 (`launcher_dirs`)・vitest +24・pytest +13

## [0.63.0] - 2026-09-05

- New: **ランチャーの Codex モデルに GPT-6 Astra が並んだ** — 「Astra (flagship)」が既定の次の行。Sol は「Sol (5.6 fallback)」に改名し、Terra / Luna はそのまま
  - GUI (`agentCatalog.ts`)・PowerShell・bash の 3 か所を同じ順で更新。契約テスト (`test_launcher_catalog_contract.py`) の行番号ケースも新しい並びに合わせた
  - effort に **ultra** (自動タスク分割・Codex CLI 0.153〜) を追加。claude-codex 側の effort は従来どおり (ultra なし)
- New: **ailog が GPT-6 Astra を知った** — 価格表 (`price.rs`: $10 / $50 / キャッシュ読み $1) と系列色の梯子 (`modelColors.ts`) に `gpt-6-astra` を最上段として登録
  - openai の梯子は 5 段のまま (Astra / Sol / Terra / Luna / 5.5)。GPT-5.4 は legacy として 5.5 の淡色に寄せた — 6 段にすると隣接段の色差が床 (ΔE 0.035) を割るため (実測 0.032 → 5 段で 0.041)
  - バックグラウンド AI のプリセットにも Astra を追加 (既定は luna のまま)

## [0.62.0] - 2026-09-04

起動メニューを、端末の中で動く文字メニューから React のペインへ置き換えた版。

- New: **新しいペイン・タブの起動メニューが React のペインになった** — マウスでもキーボードでも完結する
  - 何も打っていないときは「新規に起動 / Web / 開発 / 案件 / 続きから」の 5 セクション。各セクション 5 件までを出し、右端の「すべて (N)」で残りを開く
  - 検索語を打つとセクションの壁が消え、全体を横断した 1 本のリストになる。検索の当たり方は `Ctrl+P` の Resume と同じ設定を共有するので、同じ語で結果が食い違わない
  - 幅 240px を最悪ケースとして設計した。長いラベルは末尾ではなく中間を省略する (末尾に識別子が来る案件名を切らないため)
  - **起動処理は `launcher.sh` / `launcher.ps1` に残した。** ペインは「何を起動するか」を環境変数に載せて渡すだけなので、セッション ID の採番も作業ディレクトリの復元も従来の経路のまま
- New: **「続きから」の行と、設定「ランチャー」タブ** — 直近のセッションを数件だけ出し、使わない行は設定で隠せる
  - 一覧は `Ctrl+P` と同じ API を見るので、両方で表示が食い違わない
- Change: 「その他 → 従来のランチャー」の逃げ道を外した (移行期の足場。文字メニュー自体は `launcher.sh` / `launcher.ps1` の中に残る)
- Fix: **モデルと effort の選択が、ブラウザ側の描画に取られていた**
  - `<datalist>` のポップアップと `<select>` はブラウザが描くので、いちばん長い選択肢に合わせて横へはみ出し、暗いテーマのペインに明るい配色のまま出ていた。どちらもペインの中で折り返すチップに置き換え、色はテーマから取る
- Fix: **選択が動かず、どれを選んでいるか見えなかった** — 押したボタンがフォーカスを奪い、矢印キーがペインに届かなくなっていた
- Fix: **ランチャーペインが、描かれた場所でフォーカスを取れなかった**
- Fix: **「続きから」の行が、フォルダ名ではなくプロンプトを二度並べていた。プレビューも文として読めるようにした**
- Fix: **agy のペインで、エスケープ列の途中で保持していたバイトが落ちていた**

## [0.61.0] - 2026-09-03

ワークスペースが1クリックで作れるようになり、Web タブが5サービスに増えた版。Web ペインの「開くたびにログインし直し」は仕様ではなく不具合だったので直した。

- Fix: **Web ペインのログインが、閉じるたびに消えていた**
  - WebView2 に渡していたプロファイルのパスが `\\?\C:\…` という拡張長の綴りだった。
    Chromium はこの綴りの下に Cookie の保存先を作らないので、**Cookie は全部メモリだけ**に
    なり、アプリを閉じた瞬間に消えていた
  - 履歴・favicon・保存データはブラウザ側が書くので普通に残る。だからプロファイルは
    効いているように見えて、症状は「ログインだけ毎回消える」としてしか出なかった
  - 綴りを直した。同じ Edge・同じページで綴りだけ振った対照実験では、普通の綴りだけが
    Cookie の保存先を作る
- Fix: **「Google で続ける」を押しても、何も起きなかった**
  - ポップアップを受け取る口を用意していなかったため、認証の窓は**開かれる前に
    捨てられていた**。Google が埋め込み画面を拒むのとは別の、独立した原因
  - 認証のホスト (accounts.google.com・appleid.apple.com・auth.openai.com・x.com など) は
    ペインの中で開く。それ以外の外部リンクは今までどおり OS の既定ブラウザへ
- New: **Web タブが5サービスになった** — ChatGPT / Gemini / Grok / Claude.ai / NotebookLM
  - ランチャーから直接選べる。`web-chatgpt` のような起動キーでも開く
  - **Google 連携の4つ (ChatGPT・Gemini・Claude.ai・NotebookLM) はプロファイルを共有する。**
    Google に1回入れば4つとも通る。Grok は X のアカウントなので別扱い
  - 文章を載せる先 (`web-push`) はサービスごとに持つ。分からないサービスには
    黙って別の場所へ入れず、対応していないと言って止まる
- Change: **常時出ていた「初回ログインは別の窓で〜」の帯をやめた**
  - **ログイン済みなら何も出ない。** 未ログインのときだけ「別の窓でログイン」ボタン付きで出る
  - そのボタンは、ペインを閉じてプロファイルが空くのを待ってから、**WebView2 が実際に読む
    フォルダ**を指して実 Edge を開く。閉じるとペインが開き直る
- New: **ワークスペースが + ボタン1回で作れるようになった**
  - いま見ているフォルダ (動いているペインの現在地) を引き継ぎ、そのフォルダの名前が付く。
    同じ名前が続くときは番号を振る
  - レイアウト・フォルダ・モデルを決めたいときだけ、隣の ⌄ からダイアログを開く。
    Ctrl+Shift+N が速い方、Ctrl+Shift+Alt+N がダイアログ
  - ダイアログのエージェント一覧はランチャーと同じ一覧を読むようになった。
    6月に終了した Gemini CLI が残っていて claude-codex も agy も出ない、という
    ずれが解消した
- New: **ランチャーからモデルと effort を選べるようになった**
  - 行の上で → か `m` を押すとモデル → effort と聞いてから起動する。
    Enter と数字キーは今までどおり (CLI の既定のまま起動する)
  - モデル一覧を公開していない Grok と claude-codex の open backend 向けに、
    最後の行で ID を直接打てる
- Fix: **テスト機 (`--profile`) が、窓は出るのに何も描かなかった**
  - WebView2 はデータフォルダ1つにつきブラウザプロセス1つなので、フォルダを
    持たないテスト機が本番機のプロセスに相乗りしていた。ペインの中身は動いて
    いるのに画面には出ない、という形で出ていた
  - プロファイルごとに専用のフォルダを渡すようにした。設定も本番機から独立する

---

## [0.60.5] - 2026-09-02

ペインの端で起きていた3つのことを直した版。入力欄に書いた文章が消えて Enter だけ届く事故が止まり、ランチャーの「ChatGPT (Web)」が本当に ChatGPT を開くようになった。

- Fix: **下の入力欄に書いた文章が消えて、Enter だけ届くことがあった**
  - 本文と Enter を同じ書き込みキューに積んでいたため、先に別の書き込み
    (キー入力・マウスの動き) が処理中だと、両方が1回の書き込みにまとまって
    ターミナルに届いていた。受け取った側は貼り付けが確定する前に Enter を
    処理するので、本文だけが消える
  - ダッシュボードの返信欄と同じ経路に変えた。本文を書く → ペインが写し取って
    落ち着くのを待つ → Enter を別の書き込みで送る → 届いたか確かめる
  - フォーカスがターミナルへ戻るのは送信が済んでから。次の文章を打ち始めて
    いるときは戻さない (打っている途中の文章が半分に割れるため)
  - 届いたか確認できなかったときは黙って成功にせず、その旨を出す
- Fix: **ランチャーの「ChatGPT (Web)」がシェルのタブを開いていた**
  - Web タブを作れない経路を呼んでいた。`--split` なしの spawn はタブを
    ターミナルとして作るので、どのプリセットを指定しても捨てられていた
  - `web-open --replace-anchor` に変更。1〜6番がシェルをそのプログラムに
    置き換えるのと同じで、ランチャーが動いているタブ自体が ChatGPT になる
  - Web タブを作れない経路に Web を頼んだときは、黙って違うものを開かずに
    理由を返して止まる
- Change: **タブの右上から地球儀のボタンを外した**
  - Web タブを開く導線はランチャーの「ChatGPT (Web)」に一本化した
  - Web ペインの機能そのものは変わらない

---

## [0.60.4] - 2026-08-31

エージェントが hook で自分の状況を知らせるようになり、気づきの並びが「誰を待たせているか」で決まるようになった版。

- New: **エージェントが hook で状況を知らせるようになった**
  - Claude Code / Codex / Grok の設定に mycmux の hook を入れる。エージェント側の
    設定は壊さない (既存の項目は順序ごと保ち、mycmux の印がついたものだけを入れ替える)
  - hook が失敗しても、エージェントは止まらない。ソケットが繋がらない・応答が遅い・
    許可がない、いずれの場合も黙って何もせずに戻る
  - 質問に答える経路 (`agent_prompt_try_answer`) と、答えようとした相手がまだ
    同じ起動かを確かめる経路 (`agent_prompt_is_current_launch`) を用意した
- New: **Web ペイン (ChatGPT) に外から文章を渡せるようになった**
  - ターミナルから `web-open` / `web-list` / `web-push`。入力欄に文章を載せる
  - **送信はしない。** 載せるだけで、読んでから自分で Enter を押す
  - 長すぎる文章は載せずに断る。半分だけ貼られた指示のほうが危ないため
- New: **調整が要る件がダッシュボードのカードに出るようになった**
  - session-board が出した調整事項を読んで、気づきカードとして並べる
  - 読めなかったとき・一部しか見えていないときは、黙ってゼロ件にせず
    「観測が不完全」と分かる形で出す
- New: **ミニマップから新しいウィンドウへ引き出せるようになった**
  - これまでは引き出そうとしても、手を離した瞬間に失敗していた
  - 複数のタブを選んでまとめて引き出せる。並び順は元のペインでの順序を保つ
- New: **次の一手が、セッションを開く前に用意されるようになった**
  - 画面に出ていて手が空いているセッションの分を先に作っておく。開いた瞬間に出る
  - 1 日 12 件まで。いま見ているセッションはこの上限を使わない
- Fix: **気づきカードの並びが、種別の決め打ちで決まっていた**
  - 「誰を待たせているか」(人 / 作業 / 誰も待っていない) と重大度で並ぶようになった
  - これまでは外から届いた調整事項が、緊急でも参考情報でも同じ高さに並んでいた
- Fix: **ターンチップの ▲▼ が、出力中のペインで効かなかった**
  - 動いているペインは、目印を作る処理が画面の動きに追い越されて失敗し、
    一度失敗するとそのペインは以後ずっと目印なしのままだった
  - ▲▼ を押した時点でも画面を読み直すようにした
- Fix: **タブ再配置の案が、まるごと捨てられることがあった**
  - 判定が 1 つのタブを言い忘れただけで案が破棄され、2 分待たされて
    「案がありません」になっていた。言われなかったタブはその場に留める扱いにした
- 内部: macOS でエージェントのタブをランチャー経由で起動するようにした
- 内部: フロントのテストを node 24 に固定 (node 25 では保存まわりが壊れて 347 件落ちる)

---

## [0.60.3] - 2026-08-30

会話履歴のチップが、ダッシュボードへ飛ばす代わりにペインの中で会話を見せるようになった版。

- Fix: **会話履歴のチップが、押しても何も起きないか、ダッシュボードへ飛ばすだけだった**
  - エージェントのペインでは、画面に残っているターンを選んでも内部で処理が打ち切られ、
    実際には動いていなかった。一覧側だけ直っていて、受け取る側が拒否し続けていた
  - 画面に残っているターンは、その場でスクロールするようになった。▲▼ も
    いま見ている位置を起点に前後のターンへ動く
- New: **ペインの中で会話履歴を読めるようになった**。Claude のように画面へ履歴を
  残さないエージェントでも、チップから会話をたどれる
  - ダッシュボードで出しているものと同じ会話履歴を、ターミナルの上に重ねて表示する。
    チップで選んだターンにはその場で移動する
  - ダッシュボードへ移るのは、会話履歴の中の「ダッシュボードで開く」を押したときだけ
  - Esc で閉じる。ペインのセッションを切り替えたら閉じる
- 内部: 別のセッションが本体の作業ツリーを使っている間でもリリースできるよう、
  リリース手順が master を内包する worktree からの実行を許すようにした

---

## [0.60.2] - 2026-08-30

タブ再配置が待たずに出るようになり、返信案が見に行ったときにちゃんとある版。

- New: **タブ再配置がパネルを開いた瞬間に案を出す**ようになった。これまでは
  キャッシュが無いと AI の判定を 100 秒前後待ち、その間は空白だった
  - 作業フォルダと系譜だけで組んだ案を即座に表示する。外部への問い合わせも
    AI も使わないので待ち時間がない。**参考表示ではなく、そのまま編集も適用もできる案**
  - 案件のまとまりは「2 つ以上のタブが共有する一番深いフォルダ」で決める。
    親子でつながったタブは必ず同じ組に入り、要対応のタブは先に別の場所へ分ける。
    並べ方は列だけ・あふれたら裏タブ、という今の配置ルールと同じ
  - AI の案ができたら差し替える。ただし**こちらが編集・確認・適用に入っていたら
    差し替えず**、「AI の案ができました」と知らせるだけにする
  - タブが少ない・動かす先がない・組んだ案が検査に通らないときは何も出さず、
    従来どおり判定を待つ
- Fix: **返信案が、見に行くとほぼ毎回まだ無い状態だった**。実測では判定を 27 回
  始めて完成は 5 回、22 回はセッションを切り替えた瞬間に中止されていた
  - 切り替えても走っている判定を止めない。結果はセッションごとに残るので、
    戻ってきたときにはもう出来ている
  - 代わりに同時に走る本数で抑える (同時 2 本・待ち 4 本まで)。待ちがあふれたら
    一番古いものから捨てる
- New (土台): **エージェントの状態を、情報源を信じるのではなく証拠から決める仕組み**を追加。
  この便では画面は変わらない。今後の「呼ばれたら気づく」「その場で答える」の下地
  - これまでは Codex はログ、Claude は画面、素のシェルはほぼ勘、と情報源ごとに
    精度が違っていた。どれも単独では不完全なのに、それぞれが独立に通知を出していた
  - 判定器を1つ置き、hook・ログ・プロセスの生死・タイトルを突き合わせて
    状態を確定する。画面に出るのはその結論だけで、情報源が直接カードや通知を
    出すことはなくなる
  - 「完了」の意味を、応答が終わったこと (ターンの終わり) に限定した。
    プロセスの終了・セッションの終了は別の状態として扱う
  - 同じペインでもエージェントを起動し直したら別物として数える。
    古いプロセスから遅れて届いた報告が、新しいエージェントを完了扱いにしない
  - 通知は台帳で一度きりを保証する。アプリを再起動してログを読み直しても
    同じ通知は出ない
- 残っている遅さ: AI の案そのものは今も 100 秒前後かかる。上の即時案は
  その待ち時間を隠すためのもので、判定を速くしたわけではない

---

## [0.60.1] - 2026-08-30

タブ再配置が「分析しています…」のまま返ってこなかった件を直した版。

- Fix: **再配置の判定が返ってこなかった**。判定プロセスが手元の codex 設定
  (推論の深さ = 最大、MCP サーバ 5本) をそのまま引き継いで走っていたため、
  19〜29 タブでは 300 秒の上限までに一度も答えを出せなかった (実機ログで 5 回連続)
  - 判定プロセスは手元の設定を読まず、推論の深さを「低」に固定して走る。
    同じ 19 タブで 285 秒無回答 → 109 秒で回答、起動 48 秒 → 1 秒
  - 命名・次の一手など、裏で走る判定はすべて同じ扱い
- Fix: **先回りで用意した案が、タブを開閉すると捨てられていた**。エージェントが
  タブを開閉し続ける環境では、パネルを開いた瞬間にほぼ毎回捨てられ、
  空白のまま判定を待っていた
  - タブ構成が変わっていても前回の案を「HH:MM時点・参考表示」として即座に出し、
    その裏で判定し直す。参考表示のあいだは適用と編集ができない
  - AI の設定 (プロバイダ・モデル) を変えたときだけ従来どおり捨てる
- 残っている遅さ: キャッシュが無い初回は 100 秒前後かかる。これは判定モデルの
  生成時間そのもので、LLM を待たずに出す案は次の版で扱う

---

## [0.60.0] - 2026-08-29

タブ再配置の適用が、配置図の予告線から実ペインまで一本の動きでつながるようになった版。
あわせて、誰も答えていなかった報告インボックスの中身を撤去した。

- New: **タブ再配置の適用が、図から実画面まで飛んで着地する**ようになった。
  これまでは予告線を見せたあと、押した瞬間にパネルが消えて画面が切り替わるだけで、
  どのタブがどこへ行ったのかを目で追えなかった
  - 予告に使ったのと同じ線の上をチップが進む。線の途中で配置が実際に書き換わり、
    パネルが退いたあとも同じチップが画面に残って実ペインの位置へ着地する
  - 動くのはタブ名と色だけを持つ軽いチップで、ターミナル・xterm・PTY は動かさない。
    セッションはそのまま保たれる
  - 書き換えの前に失敗したときは、同じ線を逆にたどって元へ戻る。
    書き換えのあとに操作したときは、配置は戻さず演出だけ止める
  - 総時間は 1秒未満 (図内 0.16秒 + 着地点の確定待ち 最大0.1秒 + 橋 0.14秒 + 消え) で、
    タブが何本でも変わらない。1本の描画ループで動かし、フレームの途中で位置の測り直しをしない
  - 表示していないワークスペース宛てのタブは、実ペインが存在しないため、
    そのワークスペースのサイドバー項目へ集まって吸い込まれる
  - 設定 > 表示 > 通知とレイアウト で切れる。Windows 側で動きを減らす設定に
    していれば、それが優先されて動かない
- Remove: **報告インボックスの中身を撤去**した。「開いて答える」ボタンは
  実際には答えず、チャット列に移動して入力欄にカーソルを置くだけだったため、
  結局そこから画面を読んで打つ必要があった。押しても手数が減らない
  - あわせて、内部の集計語彙をそのまま出していた行 (対象・受領・まとめ反映・
    未受領・要判断) と、受信の3モード切替を外した
  - 気づきカードとその列は残した。エージェントの質問に列の中で直接答える
    仕組みを、この場所に入れる
  - 一括委譲の受領状況を追う仕組み自体は残っている。表示だけを外した

---

## [0.59.2] - 2026-08-29

タブ再配置が「押しても終わらない」状態を直した版。原因は3つあり、いずれも実測で特定した。
あわせてランチャーの ChatGPT が起動できなかったのを修正。

- Fix: **タブ再配置が終わらない**問題を修正。判定の実ログを解析したところ、
  103秒・158秒・179秒のいずれの実行も**答えを一度も返さないまま**制限時間で
  打ち切られていた。同じ内容を単独で走らせると 122秒 で正しい案が返るので、
  判定の中身ではなく待ち時間の設計が原因だった
  - 判定の制限時間を 180秒 から **300秒** へ。122秒に対して余裕が58秒しかなく、
    他の作業が同時に走ると超えていた
  - 各タブから送る画面末尾を 14行 から **12行** へ。8行・10行では
    タブの取りこぼしが起きることを実測で確認したうえで 12行に決めた
- Fix: **裏で先回りして案を用意する仕組みが、一度も動いていなかった**のを修正。
  発火条件が「**すべての**ペインが30秒間だまること」だったため、
  エージェントを何本も動かしている環境では、その瞬間が訪れなかった
  - 静けさを見るのは**前面のペインだけ**にした。裏で動いているセッションの
    出力は、タブの並びを考えるうえでの判断材料ではない
  - タブ構成が変わってから**3分たてば、静かでなくても1回走る**ようにした。
    条件を満たす瞬間が来ないかぎり永久に動かない、という状態をなくすため
  - これまでどおりの歯止め（利用リース・1日6回の上限・ウィンドウ非表示中は
    動かない・同時1本）はそのまま
- New: **分析中に、いま何をしているかと経過時間が出る**ようになった。
  これまでは「全ワークスペースを分析しています…」の1行のまま100秒以上
  変化がなく、進んでいるのか止まっているのか分からなかった
  - 「配置を判定中… 48秒」のように、段階と秒数を表示する
  - 1分をこえると「タブが多いと1〜2分かかります」を添える
- Fix: **ランチャーの ChatGPT (Web) が起動できなかった**のを修正。
  ランチャーは2種類あり、PowerShell 版に処理が入っていなかった
  - あわせて、失敗したときに理由を画面に出すようにした。
    これまでは失敗を捨てていたため、押しても無反応にしか見えなかった

---

## [0.59.1] - 2026-08-29

タブ再配置の適用が「動いて終わる」ようになった版。あわせてランチャーのメニュー変更が
前版で反映されていなかったのを修正。

- New: **タブ再配置を適用すると、チップが配置図の予告線どおりに動く**。
  これまでは「再配置を適用しました — 4タブ移動」という通知が出るだけで、
  配置図で見せた動きと結果がつながっていなかった
  - 動く経路は**予告線そのもの**を使う (実行時に計算し直さないので、予告と動きがずれない)
  - 適用の確定は動きの途中で1回だけ。失敗したら**同じ経路を逆にたどって**戻る
  - 同じワークスペース内の移動にも予告線を追加した (予告のない動きを見せないため)
  - 動くのはチップだけで、**ターミナル・Web ペイン・実行中のセッションには一切触れない**
  - 設定「タブ再配置の適用時に動きを表示」を追加 (既定オン)。オフにするか、
    OS 側で視覚効果を減らす設定にしていれば、これまでどおり即座に適用される
  - この版は配置図の中の動きまで。実際のペインへ引き継ぐ動きは次版
- Fix: **ランチャーのメニュー変更が v0.59.0 で反映されていなかった**のを修正。
  「ChatGPT (Web)」の追加と Fugu 2件の削除が、アプリ起動時に元のメニューで
  上書きされていた (メニューはアプリに埋め込まれており、起動のたびに書き戻される)
  - 項目を消したことでずれていた数字キー・「/」キーの割り当ても直した

---

## [0.59.0] - 2026-08-29

ChatGPT をペインの中で使えるようにした版。

- New: **ChatGPT をペインに表示できるようにした**。ランチャーの「ChatGPT (Web)」から開く。
  ターミナルと並べて置け、再起動しても復元する
  - **ログインは初回だけ、別の窓で行う**。専用のプロファイルに保存され、以降ペインを開いた時点で
    ログイン済みになる。mycmux は認証情報を読んだり移したりしない (ブラウザに預けたまま)
  - **mycmux のショートカットはペインにフォーカスがあっても効く**。ワークスペース切替 (Ctrl+Tab)・
    ジャンプ (Ctrl+1〜8)・タブ切替・ペイン間移動。コピー/貼り付け/取り消し/ズーム/新規チャットは
    ChatGPT 側に残る
  - タブを閉じるとブラウザのプロセスも消える (残ってメモリを食い続けない)
  - 非アクティブなタブ・別ワークスペース・ダイアログ表示中は自動で隠れる
    (ブラウザの画面は常に手前に出るため)
- Change: **ランチャーから「Codex (Fugu Ultra)」「claude-codex (Fugu)」を削除**した (未使用のため)。
  番号は繰り上がる

---

## [0.58.6] - 2026-08-28

タブ再配置が「分析しています…」から進まなくなっていたのを直した版。

- Fix: **タブ再配置を開くと「全ワークスペースを分析しています…」のまま止まる問題を修正**。
  v0.58.5 で入れた先回り生成の副作用で、判定を行う AI は 1 つも起動しないまま画面が固まっていた
  - **裏で用意している案に、開いたパネルが相乗りしていた**。裏の処理が詰まるとパネルも一緒に
    永久に待つ。裏は「あれば使う」だけの投機なので、詰まっていたら捨てて自分で分析する
  - 逆向きの問題もあった。分析中に裏の予約時刻が来ると、**裏の処理が待っている人の分析を中断**
    していた。投機的な処理が、人が待っている処理を中断も横取りもしないようにした
  - 走査 (タブ構成の読み取り) が返らないまま止まる経路も塞いだ。20 秒で打ち切り、
    「判定が時間切れになりました。もう一度実行してください。」と表示する
- 内部: この不具合は最初の見立てが誤っていた。裏の処理の記録は走査が終わった後に作られるため、
  走査が止まっている状態では相乗り自体が起きない。回帰テストが 3 回とも旧実装で通ってしまったのは、
  テストの中で走査を止めており、再現しようとしていた状態が発生していなかったため。
  現在のテストは走査を通し判定側を止める形で、実装を戻すと確かに落ちることを確認済み

---

## [0.58.5] - 2026-08-28

v0.58.4 でタブ再配置が使えなくなっていたのを直し、会話履歴の一覧が空だった問題を直した版。

- Fix: **タブ再配置を押すと「判定を中止しました」になり、案が出ない問題を修正**。
  v0.58.4 で入れた先回り生成が、裏の生成を止めるための処理で**画面の前で待っている分析まで
  中止していた**。端末は常に何か出力しているため、押した瞬間に中止される状態だった。
  タブを動かしても同じ経路で止まり、分析中にレイアウトが変わると完成した結果まで捨てていた
  - 裏の生成は投機なので状況が変われば止めてよいが、人が待っている分析は止めない。
    止めるのは AI のプロバイダやモデルが変わったとき (生成の前提そのものが変わったとき) だけ
- Fix: **会話履歴の一覧に行が1つも出ない問題を修正**。チップの分母 (「1/2」) と一覧の中身は
  別の場所から作られており、v0.58.2 で直したのは分母だけだった
  - 一覧を開いた後に記録が届いても、一覧が作り直されていなかった
  - 記録の取得が空だった場合に、その空の結果を30秒間覚えてしまい、行の供給元が止まっていた
    (Claude の全画面表示では設計上その場での記録を作らないため、ここが唯一の供給元だった)
  - 一覧を開いている間は取得を数回やり直すようにし、ペインを切り替えたときに前のペインの
    記録が混ざらないようにした

---

## [0.58.4] - 2026-08-28

タブ再配置の案を「押す前に」用意しておく版。あわせて通知を種類ごとに切れるようにした。

- New: **タブ再配置 (整理) の案を先回りで用意する**。これまではボタンを押してから走査・AI 判定・
  解析を始めていたため毎回待たされた。タブの増減・移動を検知して裏で案を作っておき、押した瞬間に
  出す。準備済みの案には「準備済み」と時刻が出る
  - 案が現在のタブ構成と合っていればそのまま使える。時間だけ経った案は「◯時◯分時点の案」と分かる
    形で出しつつ裏で作り直す。**タブ集合が変わった案は現行案として出さない** (今はもう無いタブを
    動かす操作をさせないため) — その場合は従来どおりその場で分析する
  - 裏での生成はタブ構成が変わってから 10 秒、かつ端末の出力が 30 秒静まってから、1 本ずつ。
    1日6回まで。ウィンドウが非表示の間は動かない
  - **この機能を一度も開いたことがなければ、購読も走査も生成も一切しない**。初めて開いた時点から
    30 日間だけ先回りが有効になる
  - 準備完了の通知は出さない (下記の通知整理と同じ方針)
- New: **右下の通知を種類ごとにオン・オフできるようにした** (設定 → 通知)。
  「AIの自動処理結果」「操作の結果」「システム・接続」の3種類。
  **AIの自動処理結果は既定オフ** — 自動命名や自動掃除の完了報告は、結果が画面に見えているため
  通知が情報を足していなかった。戻したい場合は設定でオンにできる
  - **失敗・エラーの通知はオフにできない**。気づけない失敗を作らないため、`error` はどの分類を
    指定されても失敗として扱い、通知設定を無視して必ず表示する

---

## [0.58.3] - 2026-08-28

v0.58.2 で入れた集計期間の直し方に誤りがあったため、設計を正した版。

- Fix: **AI ログの期間指定が、明示的な範囲を「カスタム期間」として扱う既定の規則を壊していた**のを修正。
  v0.58.2 では「90日」のような相対期間と、それを解決した実時刻の両方を送り、呼び名の方を優先させて
  いたが、これは「明示的な範囲指定は相対期間に勝つ」という既存の規則に反していた。
  範囲を上書きするのをやめ、**相対期間を測る起点の時刻 (anchor) を渡す**方式に変更。
  1画面が1つの起点を共有するので全パネルが同じ期間を計算し、明示指定の意味も元のまま保たれる
- 内部: この誤りは Rust のテスト3件が検出していたが、v0.58.2 はそのテストを回さずに出荷していた。
  本版は Rust 964 / フロント 3,556 / Python 369 をすべて通している

---

## [0.58.2] - 2026-08-28

AI ログの集計と、ターミナル上部の会話履歴チップを直した版。どちらも「データはあるのに
画面が空」という同じ形の不具合で、原因は別々だった。

- Fix: **AI ログ分析の「この期間の記録なし」を修正**。使用量のパネルは自分の集計期間が概要の期間と
  一致するかを見て表示を決めるが、両者は別々の呼び出しで、期間を「90日」のような相対指定のまま
  渡していたため、呼ばれた瞬間の時計から別々に計算していた。1ミリ秒ずれれば不一致になり、
  67万件の記録があっても永久に「記録なし」と出ていた。1画面が1つの期間を共有するようにした
- Fix: **会話履歴チップが「1/2」と出るのに一覧が空になる問題を修正**。スクロールバックが切り詰め
  られてマークが破棄されたとき、チップに通知が行かず、分母は古い数のまま、一覧はクリック時に
  現在の数 (0件) を読んでいた
- Fix: **会話履歴の一覧が操作中に消える問題を修正**。一覧を開いている状態が「操作していない」と
  数えられ、最後のカーソル移動から 2.5 秒で閉じていた。開いている間はチップを保持する
- Fix: **一部のセッションで会話履歴チップが出ない問題を修正**。エージェント種別が確定している
  セッションにしか出していなかったが、起動コマンド・エージェントID・起動環境が claude/codex/grok を
  示していてもその項目だけ未確定なセッションがある。判定を他の箇所と揃えた
- Fix: アカウント一覧のリセット日を全行 `↻` で統一。メーターが1本の行 (Codex) だけ「リセット 9/4」と
  書き下していたため、同じ一覧の中で別種の値に見えていた
- Fix: ワークスペースの全タブを、その同じワークスペースへ4列にまとめる再配置が「列数が4を超えます」で
  拒否されていた問題を修正 (タブが抜けて空になる列を、削除前の数で数えていた)

---

## [0.58.1] - 2026-08-28

v0.58.0 のあとに見つかった、狭いペインと再起動まわりの不具合を直した版。

- Fix: **狭いペインでタブバーが畳む順を修正**。ペインを細くすると「✕ (タブを閉じる)」と「分割」が先に消え、使用頻度の低い「しおり」だけが残っていた。畳む順は しおり → ダッシュボード → 拡大 → 分割 → ✕ になり、いちばん狭い状態でも ✕ が残る
- Fix: **再起動でペインが 1 列に潰れる問題を修正**。列の情報が失われた状態から復元すると、全ペインが 1 列に縦積みされることがあった。ワークスペースは作成時のグリッド (例 1x1) を保持したままペインを分割できるため、その乖離が復元時に出ていた。余ったペインはそれぞれ新しい列に配る
- Fix: **ワークスペースの全タブを、その同じワークスペースへ 4 列にまとめる再配置が拒否されていた**。タブが抜けた元の列は削除されるのに、削除前の数で「列数が4を超えます」と判定していた
- Fix: `pane.send_text` の期待値 (attention id / セッション revision / 入力 revision) を 3 つセットで要求するようにした。1 つだけ指定すると、守られているように見えて実質ほとんど検証しない書き込みができていた。明示的な null と未指定も区別する
- New: CLI に `status` サブコマンドを追加 (`session.state_view` を厳格に検証して返す)
- New: ChatGPT 側から mycmux を読み取り専用で見る MCP プラグイン (`integrations/chatgpt-app/`) をリポジトリで管理。セッション一覧・画面の読み取り・受け渡しキューを提供し、PTY へは書き込まない
- 内部: 封印解除で落ちていた契約テストと、選択解除を実質検証していなかった Gate 4 のテストを修正。README にメール監視の設計文書と ADR を追加

---

## [0.58.0] - 2026-08-28

ダッシュボードの「整理」(タブ再配置) を封印解除した版。AI の案を見て、配置図の上でタブを動かし、適用と取り消しができる。

- New: **タブ再配置 (整理) を公開**。ダッシュボード右上の「整理」→ AI 案の一覧 (①) → 配置図で編集 (②) → 適用前確認 (③) → 適用・元に戻す。適用後は自動で確認画面に戻り、Undo バーから元に戻せる
- New: 配置図の編集は 3 経路が同じ結果になる — チップをペインへドラッグ / チップを選んで移動先ペインをクリック / 移動先ポップオーバー。複数選択のまとめ移動、「現状維持」トレイへの戻し、空グループへのドロップに対応
- New: 適用前確認は「今 | 適用後」の並置で、別ワークスペースへ移るタブは実測位置からの接続線 (宛先色 + 形マーカー + ハロー)、同じワークスペース内の移動はバッジで示す。ホバーで強調
- New: ドラッグの中断はすべて後始末つき — Esc / 右ボタン / ポインタ喪失 / ウィンドウ非表示 / 対象タブの消滅 / 案の失効 / パネル閉じ。ドラッグ中は背景ショートカット・Ctrl+ホイール (フォント拡縮)・背景クリックでの閉じを抑止し、配置図の端で自動スクロール
- Fix: 「適用」を連打しても適用は 1 回だけ (失敗後は再試行できる)
- Fix: 案を確認中にタブ構成が変わったら自動で確認をやり直す (1 回)。失敗時は理由 + 「もう一度確認する」を表示
- Fix: 案を切り替えて戻しても編集内容は保持 (「AI の案に戻す」で初期化)
- Fix: ペインのズーム時に端末へレイアウト変更を通知 (7a5fae7) / 再フィット中も表示位置を固定・ターン一覧をその場でジャンプ (6ed846b)
- Fix: アカウント計器: Grok の製品ウィンドウ名を区別 (120294d)・単独メーターのリセット日を表示 (0591a4c)・期限切れ Grok トークンの自己更新 (f1f6036)
- Fix: 永続化: 古いリクエストによる巻き戻しを防ぐ同期プリフライトの直列化、保存時スキーマ失敗の終了前確認、ディスク修復 (6430825 / 4f87468 / 5053a1a)
- New: ailog の索引をスケジュール実行し全文をマシン外へミラー (7cdccf5)
- Fix: オーバーレイ (設定・キーバインド・タブ整理・ailog・タブ再配置) がテーマ・行間・UI 拡大の設定を反映するようになった (これまで既定のダークに戻っていた)
- Fix: 適用・取り消しの復旧に失敗したときは、中途半端な配置を保存しない (終了時に確認)。新しい形式の設定ファイルで起動したときも、終了前に確認する
- Fix: 取り消しで、適用後に変えたタブ名・作業ディレクトリ・エージェント情報が古い値へ戻らないようにした
- 内部: タブ再配置の Gate 4 実操作行列 (78 件) と性能ベンチ `npm run perf:grouping` を追加。claude-codex 用 mycmux bridge と `--expect-input-revision` は v0.58.1 予定

## [0.57.0] - 2026-08-25

AIタブ命名の日本語化と、ailog・ダッシュボード計器行の改修をまとめた版。

- New: **AIタブ命名を日本語基本に**。漢字・かな・カタカナで作業内容を要約 (目安12文字)、mycmux などの固有名詞はアルファベットのまま、一般英単語は日本語化。タブ整理の判定側フォールバックラベルも同じ規則
- New: ailog の円表示・ロールアップモデル・Codex ログ帰属の改善
- New: ダッシュボードにエージェント計器行を追加 (ツール取得・占有状況の見える化・便0+便1)
- Perf: ライブブリーフが確定済み Codex transcript パスを再利用
- Fix: UI の言語宣言を ja に修正 (テンプレート由来の pt-BR が残っていた)
- 内部: タブ再配置 (AIグルーピング) の基盤コードを搭載。入口は監査完了と実機確認まで封印中

## [0.56.0] - 2026-08-24

Claude の AskUserQuestion をダッシュボード上の質問カードで読み、その場で安全に回答できるようにした版。

- New: **AskUserQuestion の選択肢をダッシュボードへ表示**。単一選択、複数選択、複数タブ、最終確認を同じカードで扱い、説明文と選択状態も画面に反映する
- Fix: 回答先をセッションID・PTY世代・状態Revision・入力Revisionで照合し、質問が更新された場合や別の入力が割り込んだ場合は送信しない
- Fix: 質問待ちの間は通常の返信欄から同じセッションへ指示を送れないようにし、数字キーもフォーカス中の質問カードだけで受け付ける
- Fix: 非表示タブの読み取り競合、古い走査結果による上書き、質問状態と通知状態のずれを抑止

## [0.41.0] - 2026-08-16

「@A @B @C を材料に、ゴールDまで」を1枚の契約にして、GO を押すまで何も動かさない形にした版。あわせて、報告インボックスの機械層が実ログでは1件も動いていなかったのを直した。

- New: **実行契約カード**。指示を出す前に「目的 / 素材 / 実行役 / 完了条件」を1枚で見せ、GO を押すまでプロセスもブランチも作らない。素材の取得状況は「対象3｜取得3｜未取得0」と機械表示し、揃っていなければ GO を止める。GO を二度押ししても、アプリを再起動しても二重に立ち上がらない
- New: **全セッション履歴の検索**。会話の意味イベントを追記専用の記録に貯め、全文検索の索引を張った。1ターンぶんの「読んだ・書いた・実行した・テスト結果・エラー」は機械だけで要約する。**ここで AI は一切呼ばない** — 「全部覚えている」ではなく「全部検索できて、出すときは必ず根拠を添える」
- Fix: **報告インボックスの機械層が実ログで動いていなかった**のを修正。実際の Claude / Codex のログからファイル変更・テスト結果・エラーが1件も作られておらず、合成テストしか通っていなかったため気づけていなかった。同じ20セッションで 0/0/0 → ファイル変更565件・テスト結果110件・エラー86件。テストらしいコマンドが走っただけ・失敗した書き込み・成功時のエラー風文字列からは**作らない**
- Fix: 配置図でセッションを掴んで動かすと、2回目以降の移動で選択の矩形に化けてペインの上に伸びていたのを修正
- Dev: 実ログに対する検索品質の評価を追加 (読み取り専用・一時DB・通常テストからは分離)。自然文 Recall@5 0.92、エラー 0.94、パス 0.72、コマンド 0.30。弱いのはコマンド、次いでパスと分かったので、次はここを測ってから直す

## [0.40.0] - 2026-08-15

ダッシュボードを「見る画面」から**指示を出して returns を受ける場所**へ。中央のチャットが最大3列になり、素のセッション画面より有利になった版。Grok Build を正式なエージェントとして追加。

- New: **中央のチャットを最大3列**。クリックで差し替え、中央へドラッグで列を追加、✕ で1列だけ閉じる。ドラッグ中は**列ができる位置に点滅する線**が出る。既に開いているセッションを持ってくると線ではなくその列が光る
- New: **タブの整理・一括クローズをダッシュボードへ移設** (タイトルバーからは撤去)。配置図の下に置いたので「見て決めて閉じる」が1か所で完結する
- New: 配置図は既定で**全ワークスペースを展開**。帯クリックで個別にたたみ、たたんだ状態は次回も覚えている。パネル幅もドラッグで変えられる
- New: **Grok Build を正式なエージェントとして追加**。ランチャー・再開・セッションID の払い出し・状態検知まで他エージェントと同じ扱いになり、アイコンも公式マークになった
- Fix: **ダッシュボードからの送信が届かない**問題を修正。エージェントごとに送信形式が違う (Claude は貼り付け、Codex は Shift+Enter) のに素のテキストを流していた。送達を確認し、確認できないときは「確認できない」と表示する
- Fix: **エージェントが画面のフォーカスを奪わない**。タブ生成・切替・復元でユーザーの手元が飛ばされることがなくなった
- Fix: 配置図でペインを掴んだとき、掴んだ対象に枠が残って選択矩形のように見えていたのを解消。掴んでいる間は浮くだけで、範囲選択はパネルの余白でしか始まらない
- Fix: 左の一覧・中央・配置図の選択が双方向で一致するようになった。画面から社内用語 (機械信号ほか) を撤去
- Fix: 見守りの経過時間が「3900分」のように出ていたのを、分→時間→日で丸める表示に修正
- Dev: 実行契約 (WorkOrder) の土台を Rust 側に用意 (画面にはまだ出ない)。SQLite の状態遷移・二重GO防止・完了判定を機械側に置き、エージェントの自己申告だけでは完了にしない

## [0.39.0] - 2026-08-15

ダッシュボードの全面再設計。**左=セッション一覧 / 中央=会話と入力 / 右=配置図**の3面に作り直し、旧レイアウトは撤去した。

- New: **3面コマンドセンター**。左で選ぶと中央の会話と右の配置図が連動する。判断待ちは常時1列で前面に出し、質問の選択肢はカードのボタンから直接返せる
- New: **配置図 (ミニマップ) をドラッグで組み替え**られる。統合・分割・ワークスペースまたぎの移動が図の上で完結し、実画面がその形に追従する。横スクロールは廃止し、展開時はタブ名がフル幅で読める縦リストになる
- New: **点線タブ (まだ立てていないセッション)**。宣言だけしておいて後から実体化できる。宣言の時点ではプロセスを起こさない
- New: **報告インボックス**。完了・テスト結果などの事実は機械が0秒でカード化し、AI の一言はあとから同じカードに足される。まとめ報告には「対象N｜受領N｜未受領M」のカバレッジ行が必ず付く
- New: **次の一手ボタン**。状態から決まる定型ボタンが先に出て、AI が具体案を後追いで足す。開くだけでは AI を起動しない
- New: @メンションで宛先を指定して配れる。会話は読みやすさ優先で整形し、ツール実行は畳み、動いているセッションは末尾のアニメーションで分かる
- New: フォント設計を役割で分離。一覧・見出し・ボタン・配置図はゴシック固定、会話の吹き出し・表・コード・端末は設定フォントに従う
- Fix: 見守り (委譲の監視) の検知結果をダッシュボードの一覧に統合し、通知の抑止理由が画面から見えるようになった。同じ語に別の時計が付いていた問題も解消
- Fix: 起動直後の空白・接続中の見え方・タブを閉じるときの確認・非表示タブの負荷を改善 (安定性改修 Phase 1+2)
- Dev: 旧レイアウト (classic) を撤去。回帰テストは vitest 1900+ / pytest 291 / Rust 600+ 規模へ

## [0.38.0] - 2026-08-14

AIログ分析の全面再設計と、テストプロファイル隔離の完遂。異系統レビュー (Sol) 2周を経て CRITICAL/HIGH 全件を解消してからの配信。

- New: **AIログ分析をタブ5本から2つの面へ統合**。「使った量」(ログから機械的に数えた事実) と「わかったこと」(LLM 要約由来の解釈) に分け、各面は3段 (いつ・どれだけ / 何に / 使い方で変わる？、その日のまとめ / 何度も起きてること / 決めたこと)。日別チャートの棒から「その日のまとめへ」、繰り返しの罠から「その日の内訳へ」と面をまたいで飛べる
- New: 画面の語彙を統一 (コスト相当・キャッシュ率・算入外)。backend の英語識別子・英文注記は日本語表示に変換し、空表示は「なぜ空か+何をすれば出るか」型に統一
- Fix: **開くだけでは LLM を一切起動しない**設計を実配線テスト (API 呼び出し回数の実測) で契約化。段ごとの遅延読み込み・表示だけの操作で backend 再集計しない action 単位の loader に整理
- Fix: 分類・検索の切替で結果が空のままになる/「さらに読み込む」が同じページを繰り返す/閉じた詳細が復活する、を修正。セッション詳細は真のオーバーレイに復帰
- Fix: 個別セッション要約に排他ロックを導入 (連打・別ウィンドウ・一括要約との競合で LLM が重複起動しない)。ロック自体の自己デッドロックも修正
- Fix: **テストプロファイルから本番データへ届く経路を全て遮断**。アカウント切替・ペット操作・ログイン staging はプロファイル中 fail closed、使用量・savepoint 転送・介入ログ・ランチャー状態はプロファイル runtime 基準、リモートポートはプロファイルごとに自動採番。PowerShell 5.1 の BOM 付き CloneData も受理
- Fix: ペットギャラリーのカードはアトラス全体でなくポスター1コマを表示。poster が無い/壊れている pet も一覧から消えない

v0.36.0 は updater feed の未配信で更新ボタンに届いていないため、実質 v0.34.0 からの2段更新。並列6レーンの残作業と横断レビュー (P1 7件+P2 5件) をすべて取り込んだ統合版。

- New: **タブの複製が「同じ会話の続き」を確実に開く**。コピーは会話記録を新しい ID に複製してから再開する方式になり、元のタブは動いたまま。複製に失敗したときは日本語の警告を出して従来方式で開く
- New: **無応答で止まったエージェントを検知**。プロセス・画面・質問形状の3軸合議で「静かに死んだ」「質問で止まっている」を判定し、単独指標では停止と断定しない。ダッシュボードの委譲一覧は内部コードでなく日本語ラベル (判断待ち/レート制限で待機中/無応答で停止 など) で表示
- Fix: **ペイン入力欄のタップ誤爆を解消**。入力欄に触れた瞬間に閉じる・レイアウトが跳ねる・送信ボタンが突然現れる問題を修正。入力途中行の吸い上げはシェルペインに限定 (Claude/Codex で文字が消える危険を排除)。DevTools と衝突していたショートカットは Ctrl+Alt+I へ
- Fix: レート制限で待機中の通知が2回連続観測しても鳴らなかった問題を修正 (確認キーに現在分が混入していた)
- Fix: ステータス行が更新され続けるだけの「見かけ上の動き」で停止判定が永久に抑止される穴を修正 (意味内容の指紋を主判定に)
- Fix: 環境変数の保護 (MYCMUX_*) を大文字小文字を区別せずに適用。小文字で紛れ込んだ復元指示が Windows で有効化される穴を閉鎖
- Fix: コマンド完了だけでバッジ・通知パネルが増える残骸を撤去 (完了はダッシュボードにだけ残る)。停止状態の表示色をテーマ準拠+コントラスト保証に
- Fix: ペットギャラリーが表示されない問題 / テスト機が本番レイアウトを引き連れて起動する問題を修正
- Dev: セッション複製・停止検知・通知経路に回帰テスト群を追加 (vitest 1724 / pytest 264 / Rust 589)

## [0.36.0] - 2026-08-14

- New: **ペインごとに専用の入力欄**。ターミナルの下に textarea が付き、選択・クリックでのカーソル移動・undo が普通のエディタ同様に効く。Enter で送信、Shift+Enter で改行、IME 変換確定の Enter は送信されない。送り方は相手に合わせて自動調整 (Claude Code=貼り付け方式 / Codex=行単位 / 素のシェル=改行をスペースに畳んで半端な実行を防止)。v0.35.0 の「Backspace 2回で入力消去」はこの入力欄に置き換えて廃止
- New: **バックグラウンド AI のプロバイダ・モデルを設定化** (設定 → 作業 → AI)。タブ掃除の判定・命名、ailog の要約・日次ダイジェストの4箇所が1つの設定を読む。OFF にすると自動実行 (ailog パネルを開いただけで走る要約など) も全部止まる。あわせて設定保存のたびに remote_bind_all などが既定値へ巻き戻る潜在バグを修正
- New: **ペインを閉じる前に巻き添え確認**。閉じようとしたペインの裏で動いているエージェントタブを列挙して確認ダイアログを出す (何も動いていなければ従来どおり即閉じ)。長時間ジョブ用に `spawn-tab --detach` も追加
- New: **ペイン内タブバーをチップ化**。アクティブタブだけフル幅ピルで、残りは 26px のチップ (エージェントアイコン+状態ドット+ピン) に畳む。チップにホバーするとタブ名がフロート表示
- New: **サイドバーのワークスペース行をタブ名中心に再構成**。ペイン数ピル → ワークスペース内のタブ総数に変更し、アクティブなワークスペースだけ表に出ているタブ名を列挙 (上限4行+「ほか N」)。cwd・ブランチ行 / 直近ログ行 / 稼働ドット / 種別ドット / 完了数字丸は削除
- Fix: **タブ複製・再起動でセッションが別の会話とすり替わる取り違えを根治**。タブがどの会話を持つかを「一番新しいファイル」からの推測でなく記録ベースにし、複製は元の会話を読み取り専用のまま新 UUID へクローンして再開する。復元検証に失敗したときに同じフォルダの他人の会話へ自動接続してしまう経路も塞いだ
- Fix: **通知バッジを「人の介在がないと進まない状態」だけに絞る**。ただのコマンド完了 (`ls` が返っただけ等) や「> 1. ...」のような番号付きリスト行の誤検知でバッジが増えなくなった。完了はダッシュボード側の分類にだけ残る
- Fix: 再ログイン監視が、同一タイムスタンプ tick 内 (Windows は約15ms) に書き換わった認証ファイルを「変化なし」と誤判定して新アカウントを取り込めない問題を修正 (v0.35.0 の Release CI が落ちていた原因もこれ)
- Fix: `pane.send_text` (Enter なし) が未検証の書き込みを送達済みのように報告していたのをやめ、`unverified: true` を明示。`--key enter/esc/ctrl-c` などのキー送信も追加し、画面差分での到達確認を通す

## [0.35.0] - 2026-08-14

- New: **Backspace を軽く2回叩くと入力を丸ごと消す**。長い指示を打ち直したいときに Backspace を押しっぱなしにする必要がなくなる。押しっぱなし (キーリピート) では発動しないので、従来の1文字ずつの削除はそのまま。消した直後に「元に戻す」トーストが出て、消した文字列をそのまま打ち直せる (カーソル移動や履歴呼び出しをした後など、復元が保証できない場合は元に戻すを出さずに消去だけ行う)。設定 → 通知とレイアウト → ターミナル入力 でオフにできる
- Fix: 12時間表記のロケール (英語圏の設定) で使用量の枠リセット時刻が「04:00 PM」と表示され、行幅を押し広げていた問題を修正。24時間表記に固定
- New: **止まっている委譲セッションをアプリが自動で見つける**。ダッシュボードに session-dispatch の台帳を表示し、既定10分ごとに全タブと台帳を巡回して「判断待ち」「完了・未確認」「停止」「ログ未作成」などを分類して通知する。設定 → 自動化 で間隔・停止とみなす分数・通知の有無を変更できる。同じ件を2回連続で検知してから通知するので、一時的なもたつきでは鳴らない
- Fix: **枠切れ (レート制限) で待っているペインを、休眠処理が60分後に終了させてしまう問題を解消**。画面から「打ち止め」と「上限が近い」を読み分けて区別し、打ち止めのペインは休眠対象から外す。台帳側でも「停止」ではなく「レート制限で待機中」として扱うので、自動化が無駄な催促を送って枠をさらに消費することがない
- Fix: 使用量パネルでモデル別の枠が増えた行の「使用中」ラベルが右に押し出されて見切れる問題を解消。枠が4つ以上並ぶ行はバーを詰めて1行に収める
- Perf: AI ログ分析のデータベース接続を読み取り用と書き込み用に分離し、すべてのコマンドが毎回スキーマ初期化と書き込みロックを取っていたのをやめた (「database is locked」の主因)。要約が読む会話ログにも上限を設けた
- Fix: Windows のテスト実行スクリプトが cp932 コンソールで文字化けエラーを起こし、テストが1件も走らないまま終了コード0で終わる問題を修正

## [0.34.0] - 2026-08-13

- New: 使用量にモデル別の週次上限を表示するようにした (Fable / Opus / Sonnet)。従来は 5時間・7日の合計枠しか出ておらず、特定モデルの枠だけが 90% に達していても気づけなかった。頭文字が同じモデル同士で行が混ざる問題もあわせて解消
- Change: AI ログ分析の裏側を作り直し中 (画面はまだ従来のまま)。要約の対象からサブエージェントのセッションを除外 (直近7日の待ち行列の 57% がこれだった)、状態表示のポーリングがデータベースを開かないように変更、1クリック要約の失敗が記録されるようにした
- Perf: 学びタブの重複検出を近似候補生成に置き換え (実データで約270万回の比較が発生していた)。日次ダイジェストの生成が他の処理を止めないようにした

## [0.33.0] - 2026-08-13

- New: ダッシュボードのセッション詳細に「チャット」タブ。会話を ChatGPT 風の吹き出し+markdown 描画で読める
- Fix: AI ログ分析の3大バグを根治 — ①実行ボタンを押しても無反応に見える (エラーが直後のポーリングに上書きされていた) ②一度失敗すると再起動まで赤エラーが消えない ③ダイジェスト生成中に日付を送ると永久に「生成中…」のまま。あわせてポーリングを1本化 (イベント正常時は4秒間隔)
- Dev: mycmux ペイン内から pytest を実行すると socket テスト10本が本物のアプリに接続して落ちる隔離バグを修正

## [0.32.0] - 2026-08-13

- New: AI ログに「使用量」タブを新設し既定タブにした。日別の絶対量バケット (ローカル時刻基準) でトークン/コストの実量を見る。エラー表示はタブごとに分離

## [0.31.1] - 2026-08-13

AI ログ分析 (ailog) の全面修復 — 「要約が一度も走らない・会話が文字の羅列」の解消。(v0.31.0 はコマンド登録漏れを CI が検知したため欠番)

- New: テストプロファイル。`mycmux.exe --profile <name>` で本番と完全分離 (設定・socket・ailog DB・シングルインスタンス枠) のテスト用インスタンスを並行起動できる。TEST バッジ表示・updater 無効・使用量ポーリング停止 (本番アカウントのトークン回転を守るため)。`scripts/test-profile.ps1` が準備から起動まで行う。リリース前の実機確認用

- Fix: 要約の自動実行が起動直後から永久に始まらなかった (参照先がプロセス内カウンタで、再起動のたび 0 に戻る鶏卵構造)。DB の未要約件数を見るようにした
- New: 既定タブ「振り返り」から要約を実行できるようにした (未要約 N 件の表示・実行ボタン・進捗・直近エラー)。ダイジェストが空のときは理由 (先に要約が必要 / エラー) を表示する
- Fix: 要約開始前に未要約全件の生ログ (数GB) を読み切ってから始まる無反応を解消 (件数ベースの見積りに変更)
- Fix: 一度失敗したセッションが二度と要約されなかった。15分の再試行窓×3回上限で自動再試行する
- Fix: codex セッション (全体の約7割) の要約素材が空になり「素材から判断できない」ばかりだった。codex ログの読み取りに対応
- Fix: 要約器自身の codex セッションが集計を汚染し要約対象として自己増殖していた。内部セッションとして除外し既存分も遡及修正
- New: 要約対象の既定を「直近7日」に (7日/30日/全部の切替つき)。並列2本で処理
- New: セッション詳細に会話ビューを新設 (発話の役割分離・ツール呼び出しは折りたたみ・長文はクランプ+展開・上限200メッセージ)
- Fix: タイトル・一覧の「冒頭プロンプト300字垂れ流し」を廃止 (AIタイトル → 要約の目的 → 先頭1文の優先順)。未要約の行は「未要約」チップ表示
- New: セッション詳細から1件だけ即時要約できるボタン (「F3 で追加予定です」文言の廃止)
- Dev: Rust テストビルドに RAM ゲート (空き5GB未満は待機・8GB未満は -j 2) — 量産ジョブとの同居で BSOD を踏まないため

## [0.30.1] - 2026-08-13

セッション取り違え (別タブの会話を横取りする問題) の根治。

- Fix: エージェント委譲 (session-dispatch) で立てたタブが、次回起動時に同じフォルダの別タブの会話を取り込んでしまうことがあった。原因は handoff 起動が子のセッションIDを記録せず壊れたID (`claude-handoff:<親ptyID>`) を保存し、復元検証の失敗時に `claude --continue` へ降格していたこと。handoff 起動でも決定的なセッションIDを採番して `--session-id` 付きで起動するようにした (bash/PowerShell 両ランチャー)
- Fix: 保存済みの壊れたセッションIDは設定読み込み時に自動浄化するようにした (`:` や空白・パス区切りを含むIDは永続化もしない)
- Fix: セッションIDマッピングの解釈を fail-closed 化。不明な種別の行を丸ごとIDとして採用しない
- Fix: セッション追跡 (起動4秒後の後追い記録) が、起動より古いログファイルや `--session-id` 明示起動を誤って上書きしないようにした。codex 追跡も起動時刻より新しい rollout に限定
- Fix: タブの spawn が失敗したとき、PTY 未起動のタブが残って後から古い委譲指示を再実行する「孤児タブ」問題を解消 (失敗時ロールバック)

## [0.30.0] - 2026-08-12

ペット表示の基本バグ修正とダッシュボードの全面減量。(0.22〜0.29 の履歴は本ファイル未記載・GitHub Releases 参照)

- Fix: 設定を開いて閉じるとワークスペースのキャラクターが変わって見えることがあった。原因は外部ペットのカタログを設定のペットタブを開いた時にしか読み込んでいなかったこと。起動時に読み込むようにし、外部キャラの割り当てが最初から正しく表示されるようにした。「固定」モードの新規ワークスペースが Clawd に落ちる問題も同根で解消
- Change: ダッシュボード (Ctrl+Shift+G) を「要対応ファースト」に作り直した
  - ヘッダーを1行に集約。状態の件数表示 (要対応/作業中/更新なし/完了) はクリックでそのままフィルタになる
  - 完了・待機・未起動のセッションは末尾の「完了 N件 ／ 待機・未起動 M件」1行に折りたたみ (クリックで展開。完了フィルタ選択時は自動展開)
  - カードの頭行を整理 (位置バッジと作業中/完了ピルを削除)。状態レールを太くし、名前と指示文の文字を大きくした
  - 並びは要対応順に固定し、ワークスペースの絞り込みはプルダウンに変更
  - キーボード操作 (j/k・Tab・Enter・1/2/3・/・Esc) は従来どおり

## [0.21.34] - 2026-08-09

右クリック凍結の根治とマウス操作での切り離し + アカウント表示の安定化。

- Fix: 右クリックでアプリが固まることがあった。原因は WebView2 のネイティブ右クリックメニュー (モーダルループ) と、フォーカス再取得のリトライ・選択テキストの同期コピーが同時に走る競合。ネイティブメニューを全面的に抑止した (アプリ内メニューと右クリック時の選択コピーはそのまま動く)
- New: ワークスペース行にマウスを乗せると「⋮」ボタンが出るようにした。左クリックで色・名前変更・「新しいウィンドウで開く」のメニューが開く (右クリック不要に)
- New: ドラッグで切り離せるようにした。サイドバーのワークスペース行、またはペインのタブ/ペインをウィンドウの外までドラッグすると「⬈ 離すと新しいウィンドウで開きます」の表示が出て、離した位置に新しいウィンドウが開く。セッションは動いたまま移る
- Fix: アカウント一覧の並び順が取得のたびに入れ替わっていた。プロバイダ → 登録済み → 名前順の固定に変更 (エラーや使用率では動かない)。「使用中」は行の表示で示す
- Fix: 「使用量を取得できませんでした」が自動回復しないことがあった。CLI がトークンを更新した直後のアカウントは 401 が返り続けるため、Codex と同様に「401 → 保存トークンを更新 → 1回だけ再試行」を Claude 側にも追加。非使用中アカウントのトークンも定期的に回るので「要再ログイン」への転落も減る
- Fix: 429 の取得停止が一度発生すると、停止時間が伸びたまま戻らなかった。取得成功でプロバイダ全体の停止履歴もリセットするようにした
- Fix: アカウントが多いと一覧の後ろの方が毎回「今回の取得は見送りました」になっていた。1回の取得上限を 8→12 に引き上げ、見送られた行は次回先頭で取得するようにした
- New: リセット時刻を表示するようにした。一覧のバー横に 5h/7d のリセット (今日中なら時刻、以降なら日付)、詳細 (設定 > 使用量) では 4枠それぞれの下にリセット時刻。詳細のリセット列が常に「—」になるバグも修正
- New: モデル別の使用枠 (Fable 等) が API に含まれる場合、詳細画面のアカウント行の下に枠名・使用率・リセット時刻を自動で列挙する (新モデルが増えてもアプリ更新不要)
- Change: 各行に取得時刻を表示。429 の再開時刻は推定値のため「頃に再開」表記にした

## [0.21.33] - 2026-08-09

ワークスペースを別ウィンドウに切り離せるようになった (マルチウィンドウ第1弾)。

- New: サイドバーのワークスペースを右クリック →「新しいウィンドウで開く」で、そのワークスペースを独立したウィンドウに移せる。実行中のセッションはそのまま動き続ける (プロセスの起動し直しはしない)
- New: 切り離したウィンドウを閉じると、中身はメインウィンドウに戻る。ウィンドウが強制終了された場合も、次にメインが引き取る
- Change: 保存されるワークスペース一覧は全ウィンドウ分の合計になる。切り離し中でもスマホからの一覧表示は従来どおり全件見える
- 注記: ウィンドウの配置と割り当ては再起動時には保持されない (次のフェーズで対応)。アプリを再起動すると全ワークスペースがメインウィンドウに戻る

## [0.21.32] - 2026-08-09

アカウント切り替えが「うまくいったりいかなかったり」する問題の修正。

- Fix: 保存したログイン情報が使えなくなり「要再ログイン」から戻らなくなることがあった。原因は、そのアカウントを使っている間に CLI 側がトークンを更新すると、mycmux が保存している控えが無効になること。ログイン情報の変化を常時監視して控えを自動更新するようにした
- Fix: 使用量の取得が拒否されたとき、そのアカウントが実際にはログイン中だった場合は、控えを取り直して次回に回すようにした (以前は「要再ログイン」で固定されていた)
- New: 切り替えた直後に別のアカウントで上書きされた場合、警告を出すようにした。実行中のセッションがトークンを更新すると切り替えが巻き戻ることがあるため、その旨と対処 (実行中セッションを終了してから切り替え) を表示する

## [0.21.31] - 2026-08-09

タブまわりの自由度を Chrome に寄せた第2弾。

- New: ペイン内のタブをドラッグで並べ替えられるようにした。挿入位置にアクセント色のラインが出る。ピン留めタブは先頭固定のまま (ピンタブ自体を引き剥がすとピンが外れる)
- New: ワークスペースに色を付けられるようにした。サイドバーの行を右クリック → 8色から選択。行の左端の色バーとタイトルバーのドットに反映され、再起動後も保持される
- Fix: タブの×ボタンやワークスペースを閉じたとき、Ctrl+Shift+T の「閉じたタブを開く」に記録されていなかった。全ての閉じ方が対象になり、復元は元のワークスペースが残っていればそこに戻る (ワークスペース一括クローズは直近5タブまで記録)
- New: タブをマウス中クリックで閉じられるようにした (ブラウザと同じ操作感)
- Fix: サイドバーの最終ログ行が「一番最近のもの」でなく走査順の最後を表示していた
- New: 未読の入力待ち/エラー/完了リングがサイドバーのワークスペース行にも出るようにした。裏のワークスペースで起きた要対応が一目で分かる

## [0.21.30] - 2026-08-09

1.0 に向けた品質固めの第1弾 (安定・安全系8件)。

- New: 初回起動やワークスペースをすべて閉じたときに、ようこそ画面を出すようにした。「ホームで新規ワークスペース」「フォルダを選んで開始」「詳しく設定して作成」の3ボタンから始められる (これまでは設定モーダルが強制的に開くだけだった)
- Fix: エージェントの続きから再開 (resume) が、起動直後のタイミング次第で新規セッションに化けることがあった。起動パラメータを接続時点の最新値で読むよう修正
- Fix: ワークスペース保存が失敗したとき、次の操作が起きるまで再試行されなかった。5→15→30秒のバックオフで自動リトライする
- Fix: リリース版では panic やエラーがどこにも記録されず消えていた。~/.mycmux/diag.log に残る (パニック・保存失敗・ソケット/リモートの起動失敗など18ヶ所)
- Fix: 履歴パレットのパス短縮表示が特定PCのユーザー名を前提にしていた。実行時にホームを解決する
- Change: ローカル自動化ソケットにトークン認証を追加した (~/.mycmux/mycmux.token を全リクエストに添付。旧クライアント併用時は環境変数 MYCMUX_SOCKET_AUTH=off)
- Change: リリース工程に updater feed の署名 key-id 検証を組み込んだ。署名鍵の食い違い (v0.21.16 の更新失敗事故) と feed ミラーの黙殺スキップを機械検出する

## [0.21.29] - 2026-08-09

ペインのタブを「ピン留め」できるようにした。

- New: ペインのタブ帯にピンボタンを追加した。ピン留めしたタブは、ほかのタブやペインをドラッグで合流させても表示を奪われず、タブ帯の先頭に固定される。解除はもう一度ピンをタップ、タブの右クリックメニュー、または Ctrl+Alt+P
- Change: ピン留めしたタブをほかのペインへ移すとピンは外れる (移動先には引き継がない)。ピン留め状態は再起動後も復元される
- Change: タブ掃除パネルを単一リストのチェックボックス方式に作り直した。バッジ表示と常時スキャンを廃止し、AI 判定は提案のみに変更
- Change: 内部整理 (フロントの重複処理の統一・Rust の共有ユーティリティ化・不要コードの削除)

## [0.21.28] - 2026-08-08

- Fix: 使用量の取得が 429 クールダウンに入っている間、メーターが空になっていた。前回取得値を減光表示+取得時点の注記つきで出し続けるようにした。クールダウン中は警告ドットの対象から外した

## [0.21.27] - 2026-08-08

タイトルバーで隣り合っていた「CLI アカウント」と「使用量」を 1 つにまとめた。

- New: タイトルバーのアカウント表示と使用量メーターを 1 つのボタンに統合した。クリックすると、いま使っているアカウントとその使用量、切り替え先の候補が同じパネルに出る
- Fix: これまで使用量メーターの中で 2 種類の数字が混ざっていた。バーの数字は CLI でログイン中のアカウントのもので切り替えに追随するが、アカウント別の数字は mycmux に登録した OAuth アカウントのもので追随しない。どちらがどちらか画面に書かれていなかった。使用量データに「誰の分か」を記録するようにして、パネル上で出所を明示するようにした
- Fix: ウィンドウ幅が 700px 以下になると使用量メーターが消えて、数値に到達する手段がなくなっていた。統合後のボタンは狭い幅でも残る
- New: 設定のアカウントタブを 1 つの表にまとめた。「CLI ログインだけ登録」「使用量だけ登録」「両方」の区別が一覧で分かる。従来の登録・切り替え・削除の操作は折りたたみの中にそのまま残している
- Change: アカウントの結び付けは、内部 ID の一致を優先し、次にメールアドレスの一致を見る。同じメールアドレスの候補が複数あるときは結び付けず、その理由を表示する。どのアカウントの分か特定できない使用量は、どの行にも表示せず別枠に出す
- Change: Codex はアカウントごとの使用量を取得する手段がないため、その旨を画面に明記した。数値が出ないのを不具合と誤解しないようにするため
- Change: アカウント情報の取得間隔を 1 か所に集約した。ウィンドウを再び前面にしたときの更新にも対応し、使用量の取得は無駄打ちしないよう間隔を空ける

## [0.21.26] - 2026-08-08

CLI アカウント切り替えとタブ掃除の使い勝手を、コード監査で洗い出した 39 件すべてについて直した。

### CLI アカウント切り替え

- Fix: 現在使用中のアカウント (✓ が付いている行) を押すと、実際には切り替え処理が走っていた。認証ファイルを書き戻し、バックアップの世代を 1 つ消費していた。押しても何も起きないように 3 か所で塞いだ
- Fix: 定期取得が、切り替え失敗などの操作エラーを最長 60 秒で勝手に消していた。取得由来のエラーと操作由来のエラーを分離し、操作エラーは自分で閉じるまで残るようにした
- New: 切り替えの成功を通知するようにした。過去の警告には閉じるボタンを付けた
- Change: 切り替え前の確認を、日本語のボタンラベルと対象ペインの一覧を持つ警告ダイアログにした。mycmux の外で動いている CLI は検知できないことも常に明記する
- New: 登録していないログインの退避データ (孤児スナップショット) を一覧表示し、登録するか破棄するかを選べるようにした
- New: バックアップフォルダーを開く導線を追加した。切り替え後にログインが壊れたときの復帰先が画面から辿れる
- Fix: 再ログインが必要かどうかを一覧取得のたびに再判定するようにした。従来は登録した時点の判定のまま更新されなかった
- Change: 処理中の待機表示を Claude 側・Codex 側で分けた。片方の切り替え中にもう片方まで固まらない
- Change: バックエンドの更新処理を直列化した。連続操作で状態が食い違わない
- Fix: ウィンドウ幅を狭めるとバッジが完全に消えていたのをやめ、狭いときも押せる表示を残すようにした
- Fix: 初回の取得に失敗したとき、バッジごと消えて原因が画面のどこにも出なかった。バッジを残して理由を表示する
- Change: 現在ログイン中のアカウントを削除しようとしたときの警告を、通常の削除と分けた
- Change: 名前の変更を、他の場所をクリックしても確定するようにした。日本語入力の変換中に確定・取消しない
- Change: 一覧を最後に使った順に並べ、最終使用日時を表示するようにした
- Change: エラー文言を「原因 → 次にすること」の日本語に統一した (英語の内部メッセージが画面に出ないようにした)
- Change: バッジとメニューにアクセシビリティ属性を付け、開いたときのフォーカス位置を直した

### タブ掃除

- Fix: 掃除で閉じたタブを Ctrl+Shift+T で戻しても、会話が再開されず空のシェルが開いていた。復元情報を保存するようにして、元のセッションが再開されるようにした
- New: AI 判定中に経過秒と対象件数を表示し、「中止」できるようにした。中止すると判定プロセスも終了する
- Fix: 状態が変わって閉じられなかったタブが、件数から黙って消えていた。「N 件は状態が変わったため見送りました」と表示する
- New: 各行に最終出力からの経過時間と画面末尾の1行を表示するようにした。閉じてよいかを一覧のまま判断できる
- New: AI 判定を実行しなくても、ロックされていないタブを個別に閉じられるようにした (ロックの再確認は従来どおり通す)
- Change: プロンプト待機の検出を PowerShell・cmd・一般的なシェルのプロンプトにも広げた。従来は特定の記号がある場合しか候補にならなかった
- Change: ロック中のセクションを既定で折りたたみ、ラベル案を判定候補の直下へ移した
- Change: 各セクションに「何が並んでいるか」の 1 行説明を付けた
- New: AI 判定ボタンの下に、各タブの画面末尾 8 行と作業フォルダーを送ることを明記した
- New: 「再確認」ボタンを追加した。開いたまま状況が変わっても閉じ直す必要がない
- Fix: 判定に失敗したとき、押せないボタンだけが増えていた。失敗表示と再実行ボタンだけを出すようにした
- Change: 内部エラーの英語表示をやめ、日本語の原因表示に置き換えた
- Change: 作業フォルダーの表示を、末端のフォルダー名が見える省略に変えた
- New: 初回読み込み中の表示と、掃除対象が無いときの表示を追加した
- New: Ctrl+Shift+K とコマンドパレットから開けるようにした

## [0.21.25] - 2026-08-08

- New: タイトルバー (設定の左隣) に 🧹 タブ掃除ボタンを追加。死んだタブ (プロセス実体が残っていないタブ) の数をバッジで常時表示し、ワンクリックでまとめて閉じられる
- New: 掃除パネルに AI 判定 (Claude Haiku) を搭載。生存タブの画面末尾から「完了して待機中」「未送信の指示あり」「作業中」を分類し、完了タブだけを明示クリックで閉じられる。無名タブにはラベル案も提示
- Safety: 未送信の指示が残るタブ・5分以内に出力があるタブ・表示中のタブは理由つきでロックし、AI の判定に関係なく閉じない。閉じる直前にも再確認する。AI 呼び出しが失敗したときは「閉じない」側に倒れる

## [0.21.24] - 2026-08-07

- New: タイトルバーに Claude Code / Codex の現在ログイン中アカウントをバッジで常時表示し、クリックだけで別アカウントへ切り替えられるようにした。切り替えは Snapshot & Swap 方式で、通常どおり CLI でログインした状態をスナップショットとして登録しておき、以後は `~/.claude/.credentials.json` と `~/.codex/auth.json` を atomic に書き戻す
- New: 設定タブに CLI ログイン管理パネルを追加 (登録・切り替え・rename・削除)。使用量モニタリングとは別枠にした
- Change: `~/.claude.json` の書き換えは byte-span splice で `oauthAccount` だけを差し替える。残り 72 キーは byte-identical のまま保たれ、書き込み前に検証し、切り替え前の自動バックアップを 10 世代残す
- Fix: ローテートされた refresh token は identity-matched write-back で回収する。登録していないログインは orphan スナップショットとして保存されるため取りこぼさない
- Change: claude / codex が動いているペインを検出した状態で切り替えようとすると確認ダイアログを出す (lost-update 競合の警告)

## [0.21.23] - 2026-08-06

- Fix: 裏で立ち上げたペインにフォーカスを奪われ、そのあと元へ戻る「行ったり来たり」を解消。ペインを追加する処理が `activate` 指定を無視して必ずアクティブを移していた (タブ側には同じガードがあり、ペイン側だけ抜けていた)。しかもアクティブ表示だけを移してキーボードは元のペインに残るため、その状態で打鍵すると**その一打が捨てられたうえ**フォーカスが新ペインへ引っ張られていた
- Change: `--split` で立てるペインの既定を裏起動に統一。前に出したいときは `--activate` を付ける。アクティブにする場合はキーボードフォーカスも一緒に移すので、表示とキーボードがちぐはぐな状態にならない
- Fix: 別のワークスペースに居るエージェントが `--split` すると、操作中の画面が分割されていた問題。呼び出し元のペインを申告するようにし、分割はその隣に出る
- Fix: セーブポイントの引き継ぎでフォーカスが2回動いていた問題。アクティブなワークスペースとタブは即座に移すが、キーボードフォーカスは引き継ぎ文を書き終えてから1回だけ渡す。従来は最大2.5秒の待ちを挟んで2回引っ張るため、待っている間に手元へ戻ると連れ戻された

## [0.21.22] - 2026-08-06

- Fix: 引き継ぎ文が貼られた先が素のシェルだった場合に bash の構文エラーになる問題。引き継ぎ文は Enter を付けずに入力欄へ書き込まれるため、相手がエージェントでなければ操作者の Enter でコマンドとして実行される。0.21.20 で入れた文言に半角の丸括弧が含まれておりサブシェル構文と解釈されていた。丸数字と全角括弧に替えて無害な command not found に戻し、文言にシェルの特殊文字が入らないことを契約テストで固定
- Change: 引き継ぎ文で読み取りツールを名指しするのをやめる。Claude 固有の名前で Codex には存在せず、受け取った Claude が不正な引数付きでツールを呼んで失敗する例が出ていた

## [0.21.21] - 2026-08-06

- Fix: パス移動パレットの一覧が古いまま出る問題。一覧の主系である検索索引は一度作られると二度と作り直されず (更新イベントは索引ではなくフォールバック側のキャッシュしか消していなかった)、この古さは 0.21.20 以前から存在していた。索引に構築時刻を持たせ、パレットを開いたとき5分を超えていたら裏で作り直す。ドライブ直下の走査は重いので開くたびの再構築はせず、再構築中も既存の一覧を表示したままにする。あわせて表示中フォルダと展開済みフォルダのキャッシュも開いたときに読み直す
- Add: 診断ログを `~/.mycmux/diag.log` へ出力 (1 MiB でローテーション・2世代)。0.21.20 で追加した3つの計測は標準エラー出力のみで、通常起動した mycmux はコンソールを持たないため読めなかった

## [0.21.20] - 2026-08-06

- Change: D&D でセッションを渡したとき、渡された側の既定挙動を「前提を整理して停止」に変更。引き継ぎ書を読んだあと (1) 何の作業か (2) 前提・制約 (3) 未確定・要確認の点 (4) 次の一手 (案) の4点だけを提示し、指示があるまでファイル変更もコマンド実行もしない。従来は要点を出したあとそのまま作業に入っていた。新ペインを立ち上げる経路 (左右端ドロップ・カードのボタン) は従来どおり
- Perf: ドライブ丸ごとの再帰ファイル監視を廃止。従来はピン留めした `C:\` や `G:\` を `RecursiveMode::Recursive` で監視しており、ブラウザキャッシュ・Windows Update・ビルド成果物・mycmux 自身の保存まで全部が `fs_changed` として WebView へ流れていた (受け手はその大半を捨てていた)。監視対象をパス移動パレットが開いている間に読み込んだフォルダだけ・非再帰に変更
- Perf: ファイル監視に除外 (node_modules/.git/AppData/.codex/.mycmux)・毎秒20件の全体レート上限・デバウンス記録の5分TTL剪定を追加。従来この記録は一度見た親ディレクトリを永久に保持しており、長時間稼働でメモリが単調増加していた。イベント通知は Mutex を解放してから行うよう変更
- Perf: codex セッションの存在確認が呼び出しごとに `~/.codex/sessions` を全再帰走査していたのを、5秒TTLのファイル名索引に変更 (この環境で 9,726 ファイル / 1走査 37ms)。起動時の走査が codex タブ数ぶん (実測11回) から1回になる。payload の id と cwd の照合条件は不変
- Perf: 起動時に data.json を2回読んでいたのを1回に (プロセス間ロックの二重取得も解消)
- Add: 常時の重さを追跡するための診断ログ `[mycmux-diag fs_watcher]` `[mycmux-diag monitor]` `[mycmux-diag codex_index]` を60秒間隔で出力

## [0.21.14] - 2026-08-04

- Fix: パスリンクがペイン幅で折り返されるとクリックできなかった問題。真因 = パス中の実スペース (「〜合同会社 Dropbox」等) が折り返し境界に来ると行再構築でスペースが脱落し、壊れたパスがディスク照合で棄却されていた。書き込み済みセル範囲ベースの再構築で実スペースを保持 (未書き込みパディングは従来どおり除外)。回帰テスト2本追加 (実描画フレーム再現・日本語+→ファイル名)

## [0.21.13] - 2026-08-04

最高品質化 Phase B (署名: エージェント色言語) — 第2弾完結。

- Add: Claude=橙 / Codex=青 / Hybrid=緑 のエージェント種カラーをワークスペース全体へ展開 — ペインタブの2px下線 (非アクティブ55% / アクティブ85%)・下部ステータスのエージェント名横6pxドット・タブ一覧メニューの kind バッジ・サイドバーのワークスペース行に在席エージェント種の5pxドット (最大3個)
- 規律: kind 色は小さなアクセントのみ (大面積の塗りなし)。status 色 (作業中/入力待ち) とは軸を混ぜない。シェルタブは無印

## [0.21.12] - 2026-08-04

最高品質化 Phase A (質感の統一)。

- Add: 面の高さの階段 — bg < surface < surface-raised < popover をテーマ色からの派生計算で導入 (プリセット色値は不変)。ペインタブバーは raised 段、持ち上がり面には1px内側ハイライトを全シャドウに合成
- Add: 境界線の2階層化 — 区切り線用ヘアライン (`--cmux-border-hairline`) を新設。操作要素の輪郭は従来の border を維持
- Fix: メタ情報のモノスペース指定が Windows に実在しないフォント (SF Mono/Geist Mono) だったのを、ターミナルと同じ UDEV Gothic 系トークン (`--cmux-font-mono`) に統一
- Style: クロームの絵文字グリフ (✏☐📄) を単色ストローク SVG へ置換。タイポトークン (xs/sm/md) 新設

## [0.21.11] - 2026-08-04

- Fix: 「作業中」表示が実際の作業中だけ点くように。従来はエージェントプロセスが居るだけで点灯し、応答が終わって静かになったセッションも「作業中」のままだった。新判定 = 直近7秒の PTY 出力 or 画面上の作業中表示 (esc to interrupt) の検出。静かになったエージェントは無表示 (宮崎さん裁定)。「入力待ち」判定と内部ロジック (休眠判定・savepoint ガード等) は不変

## [0.21.10] - 2026-08-04

エージェント動作通知のモーション最適化 (宮崎さん FB「押すと動く・閉じると止まる・完成度が低い」対応)。

- Add: 「作業中」ステータスドットが脈動するように (ペインタブ・下部ステータスバー・ワークスペースタブ)。未使用だった agentPulse keyframe を配線。「入力待ち」は静止のまま (ペイン枠の breathing と役割分担)
- Add: オーバーレイ7画面に退場アニメーション (120ms fade-out)。`useDeferredUnmount` フック新設 — 閉じた瞬間にブツ切りで消える非対称を解消。閉じ中は inert/aria-hidden でキー捕捉も停止
- Add: 通知・完了バッジのポップイン (件数変化で再生) / CRSM 読み込みスピナー / ボタンの押下フィードバック (:active)
- Keep: PaneTabBar は className 追加のみ — 契約テスト4本 (layout/savepoint-drag/visual/no-restart) 全通過

## [0.21.9] - 2026-08-04

UI/UX 底上げ Phase 3 (コンテンツ層) — 案B 全フェーズ完了。

- Style: 設定ダイアログの左ナビ・閉じるボタンの hover を CSS クラス+motion トークン化 (useState hover の撤去・見た目は維持)
- Style: OnlinePanel の各ボタン (ビュー切替・アイコン・アクション・publish・フッター) に hover 遷移。行の持ち上げ hover はクリック可能な行のみに限定。スタイル限定 (ロジック・DOM・文字列は不変)
- Style: 日本語を含む極小テキストを 11px 床へ最終掃討 — 設定タブ (Savepoints)・OnlinePanel のチップ/ヒント/バッジ群・PublishProgress・テーマ編集パネル3枚・Usage の「要再認証」バッジ
- Keep: savepoint カード密度契約 (test_savepoint_card_density_contract) は維持 — disclosure ボタンのクラスリテラルを保全

## [0.21.8] - 2026-08-04

UI/UX 底上げ Phase 2 (クローム層)。

- Style: タイトルバーボタン・メニュー項目・ワークスペース項目・ペインタブ・ペイン操作ボタンの hover に motion トークンの transition を付与 (従来は0msで切替)
- Refactor: onMouseEnter の style 直接書き換えによる hover (TabBar・TabItem・NotificationPanel) を CSS :hover クラスへ移行。ペインドラッグ中の hover 抑止は data 属性で維持
- Add: アンカー型ポップオーバー (PathJumper・Usage・通知パネル) に共有入場アニメーション `.cmux-popover-panel`
- Style: 日本語を含む 10px テキストを 11px へ (PathJumper エラー/ヒント/パス・Usage バッジ・Artifact ツールバーのパス)。復元バッジ 9px→10px

## [0.21.7] - 2026-08-04

UI/UX 底上げ Phase 1 (計画: `docs/plans/2026-08-04-ui-uplift-plan.md`)。

- Add: デザイントークン (motion 3段 + easing / オーバーレイ blur / パレット・ダイアログの2層シャドウ) と共有オーバーレイクラス (`.cmux-overlay-backdrop` / `.cmux-overlay-panel` — blur 幕 + 160ms 入場アニメーション)。Resume パレット・設定・キーバインド・Usage・PathJumper に適用
- Fix: Resume パレットの検索が最初の1000件しか対象にならなかった問題 — 検索中も「過去分も読み込んで検索」導線を表示
- Perf: Fuse 検索インデックスを1キーストロークごとに再構築していたのをフィルタ変更時のみに
- UX: Esc は検索語クリア→クローズの2段階 / PageUp・PageDown・Home・End 対応 / 読み込み中表示 / 検索マッチ箇所のハイライト
- Style: パレットの操作要素を CSS クラス化して hover/transition を付与。日本語を含むテキストのフォント下限を 11px に

## [0.21.6] - 2026-08-04

- Fix: Resume パレット (CRSM) で検索中も一覧が最終更新順 (last_activity 降順) を維持するように。従来は検索語を入れると Fuse.js の類似度スコア順で表示され、更新順ルールが崩れていた。検索は絞り込みのみを担う

## [0.20.2] - 2026-07-25

内部リファクタ集中パック (機能・見た目の変更なし)。`refactor-instructions.md` に基づく負債返済21コミット。

- Refactor: 巨大ファイル分割 (pure move) — `pty/monitor.rs` (2,207行) を detection / git_branch / runner / transcripts / tests の5モジュールへ、savepoint 系 `commands/online.rs` + `online_publish.rs` (計約5,000行) を join / lifecycle / digest / transcript / publish / tests の9モジュールへ
- Refactor: フロント⇔Rust IPC の invoke 64箇所を型付け (payload/戻り値の型検査で「静かに壊れる」を予防)
- Refactor: savepoint publish のオーケストレーションを共有 hook `useSavepointPublish` に一元化 (OnlinePanel / PaneTabBar の重複実装を解消)
- Refactor: usage 監視の OAuth 補助関数を `usage/util.rs` に統合 (二重実装解消)
- Fix: `data.json` の backup/一時ファイル掃除失敗が黙殺されていたのを stderr に記録するように
- Fix: リモート API の状態読み込みを async ランタイム外 (`spawn_blocking`) へ退避
- Remove: ファイルエクスプローラー廃止で死んでいた store の残骸443行 / 無効化済み `windows_console.rs` / 廃止ビルドスクリプト3本 / 誤コミットされていた v0.19.2 インストーラ (27MB) を削除、`.gitignore` に `*.msi`/`*.sig` を追加
- Tests: 安全網テスト19本追加 (workspaceLayoutStore の move/split 8本・PTY reattach 分岐・socket timeout 掃除ほか)。合計 tsc 0 / vitest 419 / cargo 200 / pytest 141 全グリーン
- Docs: `docs/current-state.md` を v0.20.x 現状 (lite 廃止・公開ミラー sync 方式) に刷新。設計提案4本を `docs/plans/2026-07-25-*.md` に追加 (transcript 統合不可の確定分析・store 境界・terminal 分割・typed error — いずれも実装は未着手)

## [0.20.1] - 2026-07-25

- Fix: 日本語を含むパスがターミナルの折り返しで分断されたときにリンク化されない問題を修正 (セル幅忠実のテストハーネスで再発防止)

## [0.20.0] - 2026-07-23

ターミナルパスリンクの信頼性を大幅修正。公開リポジトリ一本化 (lite 配布終了) に伴う整理も同梱。

- Fix: **パスリンクの3症状を根治** — ①バッククォート囲みのパス (エージェント出力の定型) がリンク化されない ②Claude Code / Codex の TUI がハード改行で折り返した長いパスがリンク化されない (継続4行まで結合し、ディスク実在チェックで誤結合を抑制) ③ワークスペースやタブを切り替えて戻った後、リンクをクリックしても無反応 (キャッシュ済みターミナルが破棄済みペインのクリック処理を保持していた問題。再接続時にハンドラを張り直し、契約テストで配線を監視)
- New: 相対パス (`./` `../` `dir/file`) と `~/` もペインの現在ディレクトリ基準で解決してリンク化 (実在確認済みのみ)
- Fix: タブ一覧プルダウン (⋯ / n/m ▾) が狭いペイン (compact/micro) で見切れて読めない問題 — fixed 配置+ビューポートクランプに変更し、幅・高さも小さいウィンドウに追従
- Remove: 到達不能になっていたファイルエクスプローラーサイドバーを廃止 (開くトグルが失われた死にUI)。ファイル閲覧は OS Explorer の領分とし、mycmux はパスリンク・reveal で接続を楽にする方針に一本化。pinned_roots 基盤は温存
- Docs: 公開リポジトリを mycmux-team に一本化、lite 版の配布終了を README に明記

## [0.19.2] - 2026-07-16

セーブポイントの左右ドロップを「完全再開」既定に変更 (v0.19.1 実機フィードバック)。

- Change: セーブポイントカードを左右端へドロップしたとき、**過去セッションがそのまま復活する完全再開** (`--fork-session`・元セッションに影響なし) でその側に新規ペインを開くように変更。カードを開いたときの「完全再開」と同一機構を再利用。
- Safety: 完全再開できないカード (トランスクリプト欠損等) は従来の引き継ぎ書スタートに自動フォールバックし、警告トーストで明示。
- UI: ラベルを「左に完全再開」「右に完全再開」に更新。下部 (この会話へ引き継ぎ) と「人に渡す」は変更なし。

## [0.19.1] - 2026-07-16

Mac パリティ検証ラウンドの成果を全プラットフォームへ反映。

- Change: **ターミナルレンダラーの既定を全プラットフォームで「標準描画 (DOM)」に変更** (Mac 実機で WebGL 由来の「ペインが濃く沈む」を確定診断)。GPU 描画は設定 → 外観からの opt-in として残存。
- Change: 設定移行 (version 3) — 旧既定で webgl を引き継いでいる環境は一度だけ dom にリセット (過去に明示的に GPU を選んだ記録がある場合は温存)。
- Note (Windows): v0.14.2 で計測した WebView2 renderer の CPU 消費が気になる場合は、設定から GPU 描画を再度 opt-in してください (1トグルで戻せます)。
- UI: 設定画面の「Windows推奨」表記を既知問題の説明に差し替え。

## [0.19.0] - 2026-07-16

v0.18.0 実機フィードバックの反映リリース。DnD の細部と、ボタンの畳まれ方を作り込み。

- Fix: 自分のペインへのドラッグで「このペインのタブに追加/ペインを統合」チップが出る誤表示を廃止 (自ペインへの中央ドロップは no-op のため)。ペインごとのドラッグは自ペインの左右分割も非表示。タブのドラッグは2タブ以上のときだけ自ペイン分割を提示。
- UI: 「◯◯ へ引き継ぎ文書を渡す」チップをペイン**下部** (入力欄の上) に移動。ホバーで色が変わる操作感は従来どおり。
- Feat: **セーブポイントカードのドラッグをペイン移動と同じゾーン設計に統合** — 左右端=その側に新しいセッションを作成して引き継ぎ / 下部=この会話のエージェントへ引き継ぎ (従来のペースト・Enterで送信)。承認待ちブロック・生存確認などのガードは共通のまま。
- Feat: タブバーの優先ボタンを**1個ずつ**畳む段階設計に変更。畳まれる順=しおり (Publish) → Split → 拡大 (Zoom) → **× (Close) が最後まで生存**。新境界 250/290・210/238・170/198・130/158px (各ヒステリシス28px以上)。
- Fix (macOS): ネイティブタイトルバーをアプリ内バーに統合 (titleBarStyle Overlay) — Mac パリティ検証ループの初回修正。
- Test: vitest 385 / pytest 141 / cargo 193。

## [0.18.0] - 2026-07-16

「入力待ちが消えない・全部入力待ちになる」通知の根治+エージェントのタブ spawn を裏起動化。おまけで launcher の Gemini 枠を Antigravity (agy) に置換。

- Fix: **「入力待ち」ステータスの誤検知を根治**。矢印文字・番号付きリスト・「Enter to send / Esc to cancel」等のアイドルフッターに常時マッチしていた緩い検出パターンを廃止し、実際の承認UI (Do you want to proceed? / ❯ 1. Yes / 行末 (y/n) など12種) だけの高精度パターンに全面書き換え。過去の文字化けで死んでいたパターンも復活 (検出モジュールを純ASCIIソース化しテストで固定 — 同事故の再発を構造的に防止)。
- Fix: 「入力待ち」の解除を「承認プロンプトが2スキャン連続で画面から消えたら解除」に変更。完了後の静止画面で入力待ちが永久に残る問題を解消 (再描画中のチラつき・再通知もなし)。
- Feat: **エージェントによる同ペインへのタブ spawn (`pane.spawn_tab`) は既定で裏起動に変更**。タブは増えるがアクティブタブとフォーカスは今見ている場所から動かない。前面に出したい場合は CLI `--activate`。裏タブは PTY だけ起動 (レンダラは作らずCPU節約を維持) し、タブを開いた時にそれまでの出力をスクロールバックとして表示。UI の「New tab」ボタンは従来どおり前面。
- Change: launcher メニューの Gemini 枠を Antigravity (agy) に置換 (Gemini CLI 個人向け終了のため)。
- Test: 承認スキャナ24 / spawn裏起動・mount契約 (vitest 369 / pytest 140 / cargo 193)。

## [0.17.0] - 2026-07-16

ペイン移動ドラッグから「引き継ぎ」ができる新機能+Codex ドロップ先の不具合根治+DnD 表示の日本語化。

- Feat: **ペイン移動時の引き継ぎ** — ペインやタブをドラッグすると、他のライブなエージェントペイン (Claude Code / Codex 両対応) に「◯◯ へ引き継ぎ文書を渡す」チップが出る。ドロップすると移動はせず、元セッションのセーブポイントを自動保存 → 引き継ぎ文を相手の入力欄に書き込む (Enter で送信)。承認待ちブロック・生存確認などの安全ガードはセーブポイントドラッグと共通化 (`savepointHandoffRuntime`)。
- Fix: **Codex ペインがセーブポイントのドロップ先に出ない** — monitor.rs が Claude はプロセス検出で即通知するのに Codex だけセッションID 解決成功時のみ通知する非対称だった。Claude と同じ即時通知に修正 (Rust テストで固定)。
- Fix: **Claude Code でもたまにドロップ先が消える** — 「最前面プロセスがシェルでない」判定が、Bash ツール実行中を 5 秒間隔監視が「シェル」と観測する瞬間にターゲットを消していた。agent 在籍情報ベース (多層安全弁経由でしかクリアされない) に変更。
- UI: 書き込みプレビューを「ここに◯◯へ引き継ぎ文書を渡します（Enterで送信）」に変更し折り返し表示に。ドラッグゴーストの「右に会話…」切れも解消。ペイン移動ラベルを日本語化 (「右にペイン」「このペインのタブに追加」「ペインを統合」「新しいワークスペースへ移動」等、「3 tabs」→「タブ3個」)。
- Test: vitest 336 / cargo 193 (+1) / pytest 135。

## [0.16.1] - 2026-07-16

ターミナル内パスリンクの誤検知根治リリース。「リンクを押したら path does not exist」「本物のパスが押せない」を解消。

- Fix: 正規表現で拾ったパスリンクも含め、**全リンク候補をディスク実在チェックに通してからリンク化**するよう変更。スペース・日本語を巻き込んだ「散文ごとリンク」は実在する最長プレフィックス (本物のパス部分) に自動で縮む。存在しないパスはそもそもリンクにしない。
- Fix: 誤検知リンクが実在チェック済みリンクを覆い隠していた overlap 除外の構造を反転。重なった候補は「実在プレフィックスが長い方が勝ち」で1本だけ生成。
- Safety: 実在チェック IPC が失敗した時だけ従来の未検証リンクにフォールバック (リンクが全消えする事態を防ぐ)。既定の一括バッチ1回・キャッシュ・stale ガードは従来どおり。
- Test: 散文巻き込みの縮小・覆い隠し解消・非実在ノーリンク・IPC フォールバック・overlap 優先順のユニットテストを追加 (vitest 328)。

## [0.16.0] - 2026-07-16

v0.15.0 のコンパクトタブバーへのフィードバック反映。「狭くなると全ボタンが一気に ⋮ へ消える」をやめ、段階的に畳む設計へ。

- Feat: タブバーの縮退を full → slim → compact → micro の4段階に再設計。よく使う4ボタン (Publish セーブポイント / Split right / Zoom / Close pane) は slim・compact でも直接ボタンのまま残り、micro (250px 未満の物理限界) まで消えない。先に ⋮ へ畳まれるのは New tab / Split down のみ。
- Feat: 各境界に独立したヒステリシス帯 (560/600・360/400・250/290) を持たせ、広げれば同じ段を対称に復帰。1回の幅計測で複数段をまたいでも正しく解決する (スプリッタの高速ドラッグ対応)。
- Fix: ⋮ メニューは「いま隠れているボタンだけ」を表示 (直接ボタンとの重複なし・隠れゼロなら ⋮ 自体を非表示)。直接ボタン列と ⋮ の中身を同一の純関数 (`resolvePaneTabBarActions`) から描画。
- Test: 全境界の双方向ヒステリシス・複数段またぎ・単調性 (幅が広がるのに縮退しない)・モード×Publish有無のボタン振り分けのユニットテストを追加 (vitest 323)。

## [0.15.0] - 2026-07-15

狭いペインでタブが触れなくなる問題を解消するタブバー再設計リリース。

- Feat: ペインのタブバーにアダプティブ・コンパクトモードを追加。ペイン幅 360px 未満で「アクティブタブ名 + n/m ▾ + ⋮」表示に自動切替し、400px 以上で従来表示へ復帰 (ヒステリシスでチラつき防止)。▾ で全タブ一覧 (選択/閉じる)、⋮ に New tab / Publish / Split / Zoom / Close pane を格納し、どんなに狭くても全操作が2クリック以内。
- Feat: ペイン内タブ巡回ショートカット `Ctrl+Alt+PageDown` / `Ctrl+Alt+PageUp` を新設 (設定のキーボードショートカットから変更可)。
- UI: アクティブタブの強調 (アクセント混色背景+太字) と状態/通知ドットの拡大+コントラストリングで視認性を底上げ。従来の「狭いと Split/Zoom ボタンが消えるだけ」の CSS ルールは撤去。
- Test: コンパクト切替の純関数ユニットテストと、タブ巡回ショートカットの契約テストを追加。

## [0.14.19] - 2026-07-15

claude-codex ペインが再起動でほぼ復元されない問題の根治リリース。

- Fix: PTY monitor が claude-codex セッションを一度も帰属できていなかった問題を修正。claude-codex の実体は素の claude.exe (env で `~/.claude-codex/config` を指すだけ) のため、プロセス形状検出では常に「claude」と判定され、session id の探索先も `~/.claude/projects` 固定で、claude-codex の復元識別子が data.json に保存されなかった (実測: 保存済み agent_kind 12件すべて claude)。
- Fix: launcher が書く pane mapping (`claude-codex:<sid>`) を、検出結果が claude でも整合として受理し最優先で採用。mapping が無い場合は `~/.claude/projects` → `~/.claude-codex/config/projects` の順に探索し、後者で見つかれば kind を claude-codex として帰属。既存の claude-codex 帰属は新しい identity が見つからない限り保持。
- Test: 帰属5ケース (mapping優先/ディレクトリfallback/既存identity保持/素のclaude優先回帰/exact id > stale mapping) のユニットテストを追加 (cargo test 192)。
- CLI (0.14.18 以降の運用変更・exe 変更なし): `spawn` の既定配置を「呼び出し元ペインの同タブ」に変更 (`--split` で従来の分割)。cp932 コンソールでの UnicodeEncodeError を修正。

## [0.14.18] - 2026-07-15

Codex 委譲を「見える化」する後半リリース。エージェントが自分のペインに作業タブを開き、済んだら畳めるようになった。

- Feat: `pane.spawn_tab` を追加。ペイン内のエージェントが**自分と同じペインの新タブ**を開ける。one-shot コマンド実行 (codex exec 等・終了で自然に止まる) と、対話エージェント起動 (引き継ぎ書つき Claude Code/Codex) の2形態。
- Feat: `pane.close_tab` を追加。呼び出し側ラッパーが成功を確認してからタブを畳む用途。最後の1タブは拒否し、閉じたタブは Ctrl+Shift+T の履歴に入る (作業フォルダのシェルとして復元)。
- Safety: one-shot タブのコマンドは保存データに残らず (契約テストで固定)、閉じタブ履歴にもコマンドを記録しない。再起動や再オープンで有償の codex 呼び出しが勝手に再実行されることはない。
- CLI: `mycmux_agent_cli.py` に `spawn-tab` / `close-tab` を追加。

## [0.14.17] - 2026-07-15

ペイン内のエージェントが「目に見える新ペイン」を自分で立ち上げられるようにするリリース。「この続きを Codex にやらせて」を、裏実行ではなく画面上のペインとして実現する土台。

- Feat: ローカルソケットに `pane.spawn` を追加。ペイン内の Claude Code／Codex が引き継ぎ書・セッション引き継ぎ (CRSM handoff)・resume・新規起動の4モードで、可視の新ペインを右分割で立ち上げられる (戻り値で paneId/sessionId を受け取り追跡可能)。
- Feat: `pane.send_text`／`pane.read` を追加。既存ペインへの入力送信と画面末尾の読み取りができ、エージェントが立ち上げたペインの進捗確認・追加指示に使える。
- Feat: エージェント用クライアント `scripts/mycmux_agent_cli.py` を同梱 (標準ライブラリのみ。workspaces/panes/spawn/send/read)。
- Safety: `MYCMUX_LAUNCH_TARGET` を ephemeral env ガード (起動時 remove_var・保存フィルタ・契約テスト) に追加し、新規起動 env が data.json に残って復元時に勝手にエージェントが立つ事故を予防。
- Refactor: ソケットコマンド処理を `socketCommands.ts` に分離し、spawn 計画の組み立てを純関数化して単体テストを追加。

## [0.14.16] - 2026-07-14

セーブポイントを共有フォルダから切り離し、必要な1件だけを手渡せるローカル中心の受け渡しリリース。

- Feat: セーブポイントはこのPCのローカル領域へ保存。カードの「人に渡す」またはドラッグ中だけ現れる受け渡し先から、単一の `.mycmux-transfer` ファイルを書き出せるようにした。
- Feat: セーブポイント画面の「受け取る」とアプリへのファイルドロップから受信。Claude Code／Codexの会話、引き継ぎ書、復元情報を1件のカードとして取り込み、同じファイルの二重取り込みを防止する。
- UX: 有効期限・受信コード・共有フォルダ設定は追加せず、狭いペインでは受信・更新操作をアイコン化。ドラッグを一覧内で離した場合は従来どおり何もせずキャンセルする。
- Stability: 書き出し・受信・整合性検査をUIスレッド外で処理。再取り込み時も保存済み本文を再検証し、受信した終了時記録を通常どおりゴミ箱・期限整理できるようにした。
- Restore: 相手のPCに元の作業フォルダがなくても、ホームフォルダを代替先としてClaude Code／Codexの会話を復元。作業ファイル自体は含まれないことを警告表示する。
- Compatibility: 既存の `C:\Users\miyaz\.mycmux` 内の保存先は継続利用し、外部の旧共有フォルダは変更・削除せず参照情報だけ残す。
- Release: v0.14.15はGitHub runner固有の8.3短縮パス表記でテストが停止し、配布物を公開せず終了。パス表記に依存しない検証へ直してv0.14.16として公開する。

## [0.14.14] - 2026-07-14

セーブポイント共有の最終調整と、多数のClaude Code/Codexペインを開いた際の表示消失・入力遅延を抑える安定化リリース。

- Fix: セーブポイントを持ち上げた後、セーブポイント画面内で離した場合は何も起動・入力せずキャンセル。別workspaceへドラッグ中も元カードを保持し、復元先または書き込み先が確定した時だけ処理する。
- UX: 狭いペインでも一覧性を保てるよう、セーブポイントカードのタイトル・メタ情報・操作領域を圧縮。完全復元とClaude/Codexへの引き継ぎ文入力は、ドロップ表示とアニメーションを分離した。
- Fix: ペイン増減でレイアウト全体のkeyを変えていた処理を撤廃し、列をペイン重複から安定同定。分割操作のたびに既存xtermが再生成され、内容が一時消失する経路を解消した。
- Stability: 非表示・0×0ペインではバックエンド出力を停止し、復帰時は必要な末尾だけを一度再生。Claude/Codex双方へリサイズ再描画を送り、切断復旧の連続再同期ループを防止した。
- Perf: ターミナル出力をバイナリ転送し、ACKを32ms単位の累積送信へ集約。タイムアウト・再試行・世代交代・未解決invoke上限を追加し、多ペイン出力時の入力経路詰まりを抑制した。
- Test: TypeScript、Vitest 279件、pytest 121件、Rust release 180件、production build、差分検査を通過。ローカル導入版のversion・SHA256一致と、再起動後6ペインの復元を確認した。

## [0.14.11] - 2026-07-14

セーブポイントD&Dの書き込み先表示を分割復元と明確に分け、削除を復元可能なゴミ箱方式へ変更するUX・安全性リリース。

- UX: Claude Code/Codexへの貼り付け時は、対象入力欄の位置に「ここへ書き込む」プレビューとカーソル点滅を表示。右端・下端への完全復元は従来の分割プレビューを維持し、同じアニメーションに見えないよう分離。
- Feat: セーブポイント画面へ「保存済み／ゴミ箱」を追加。初回削除はゴミ箱へ移動し、「元に戻す」または4秒以内の二段階確認による「完全に削除」を選べるようにした。
- Safety: ゴミ箱は共有保存先の `_trash/<UUID>` に退避し、復元時の上書き、外部パス・reparse path、ID・session・checkpoint不一致、現役の最終記録が再生成済みの場合の完全削除を拒否。
- Stability: 最終記録をゴミ箱へ移した直後の再保存は新しいcheckpointへ派生。途中停止してもheadへ先行保存した同一IDを再利用し、重複・孤児化を防止。一覧再読込も世代管理と片側エラー分離で古い応答の上書きを防止。
- Cleanup: アプリ内の期限切れ整理もゴミ箱へ移動。一時残骸だけを完全削除し、旧Python cleanup CLIは確認専用に変更。
- Test: Vitest 245件、pytest 111件、Rust release 169件、TypeScript・production build・差分/format検査を通過。

## [0.14.10] - 2026-07-14

0.14.9 の実機フィードバックを反映し、セーブポイントD&Dと多分割表示の挙動を修正する緊急リリース。

- Fix: 稼働中のClaude Code/Codex中央またはタブへドロップした場合は、既存セッションの入力欄へ引き継ぎ文だけを貼り付けるよう変更。改行・Enterを送らず、新しいhandoffタブも作らない。
- Fix: ペイン右端・下端、または非AIタブへドロップした場合は、既存の「会話ごと続ける」と同じ完全復元を使い、指定方向の新しいペインで元の会話を再開するよう変更。
- Safety: 非AIタブから別のアクティブAIタブへ誤投入するフォールスルーを防止。確認・選択肢への回答待ち、ドロップ先消失、PTY未起動を再検証してから貼り付ける。
- UX: 420x220pxの最小寸法と外周スクロールを廃止。全ペインを常に表示領域内へ分割し、ターミナル内部の履歴スクロールだけを維持。
- Test: D&Dモード分岐、改行除去、ライブ対象判定、Tauri生存確認API、表示領域内フィットを単体・契約テストで固定。

## [0.14.9] - 2026-07-14

セーブポイントのドラッグ共有と、Claude Code/Codexを多重起動した際の描画・負荷を改善する安定化リリース。

- Feat: セーブポイントカードをClaude/Codexのペイン・タブへドラッグし、元のセッションへ入力を混ぜず、安全な新規handoffタブとして再開できるようにした。
- Fix: 非表示・最小化ターミナルの描画・同期を抑え、復帰時はカーソル位置と連番を使って不足分だけ再同期。UTF-8の分割出力や取りこぼしも復旧するようにした。
- Perf: ターミナルキャッシュを12件、scrollback再同期を256KiBに制限。autosave範囲を絞り、起動復元後の固定キャッシュを解放し、レイアウト通知を対象workspace内に限定した。
- UX: ペインの最小寸法を420x220pxに固定し、多分割でそれ以下になる場合は縦横スクロールへ切り替え、アクティブペインを自動表示するようにした。
- Safety: D&D先をdrop時に再検証し、cwd不明時はタブ・PTYを作らず停止。通常のペインD&Dとの競合と、期限切れセーブポイントの誤操作を防いだ。
- Test: TypeScript、Vitest 240件、pytest 102件、Rust release 163件に加え、D&D・非表示復帰・キャッシュ上限・レイアウト最小寸法の回帰契約を追加した。

## [0.14.8] - 2026-07-14

Windowsの端末描画を安定側へ戻し、ストリーミング中の重さと履歴スクロール不能を修正する緊急リリース。

- Fix: 設定名変更で失われていたWindowsの標準描画（DOM）選択を復元。GPU描画は任意選択として残し、設定名と説明を日本語で明確化。
- Perf: PTYの小分け出力ごとに可視行全体を2回再描画していた処理を削除。再接続・リサイズ・履歴再生時の再描画だけを重複排除して維持。
- Fix: Claude/Codexとして起動した通常バッファのホイールを端末履歴へ戻し、alternate bufferだけTUIへ転送。Shift+ホイールは常に履歴スクロールとして利用可能。
- UI: ペイン上部の状態表示から色付き背景と枠を外し、状態ドット・日本語ラベル・エージェント名だけのフラット表示へ変更。
- Release: 実機確認で問題が残った0.14.7の公開処理を停止し、GitHub Releaseは作成していない。
- Test: Windows描画設定の移行、ホイール経路、出力ホットパスの全画面再描画禁止、状態表示の単層化を回帰テストで固定。

## [0.14.7] - 2026-07-13

0.14.6 の背景不透明度変更を撤回し、保存済みの外観設定を全ペインで統一して使う緊急修正リリース。

- Revert: Codexだけ端末背景を90%以上の不透明度へ強制していた処理を削除。Codex・Claude・ランチャーを含む全ペインを、利用者が保存した端末背景と透明度へ戻した。
- Fix: 多重シャドウの除去、境界線によるアクティブ表示、日本語の「作業中」「入力待ち」は維持。色パレットとANSI配色は変更していない。
- Release: 問題を含む0.14.6の公開処理を停止し、GitHub Releaseは作成していない。
- Test: エージェント種別ごとの背景上書きを禁止し、全ターミナルが共通背景を使う回帰テストへ修正。

## [0.14.6] - 2026-07-13

ステータス表示の影と、Codex起動時の背景色混合を整理する視認性修正リリース。

- Fix: タイトル・ペインタブ・状態ドット・アクティブペインに重なっていた多重シャドウを除去。アクティブ状態は2pxのアクセント枠で明確に維持。
- Fix: 壁紙使用時にCodexの端末面だけ不透明度を90%以上へ引き上げ、既定62%の背景透過でTUI配色が壁紙と混ざる問題を軽減。
- UX: ステータスを「作業中」「入力待ち」に日本語化。起動メニュー内からCodexへ移った後も、実行中の種類を優先して「Codex」と表示。
- Test: フラットな状態表示、Codex面の視認性下限、起動後のエージェント名解決を固定する回帰テストを追加。

## [0.14.5] - 2026-07-13

呼称を「セーブポイント」に統一し、設定に専用タブを追加したUX整理リリース。

- Fix: 0.14.2 の描画設定リネームで、旧設定の DOM 選択が WebGL に黙ってリセットされていた問題を修正。保存済み設定の移行処理 (persist migration) を導入。
- Feat: 設定に「セーブポイント」タブを新設 (保存方法・再開方法の説明とパネルへの導線)。「スマホ・リモート操作」と機能の違いを明記して分離。
- UX: 画面・ボタン・README の呼称を「セーブポイント」に統一。設定ナビを「表示 / 作業 / 接続 / アカウント / その他」に再編し、ナビ幅を画面幅に追従 (clamp)、ラベルの折り返しを許可。
- UX: タイトルバーのしおりボタンに aria-label を付与。外観タブの描画方式の説明文を実挙動に合わせて修正。
- Test: 設定移行の unit テストと、セーブポイント/リモート操作の導線分離を固定する契約テストを追加。

## [0.14.4] - 2026-07-13

複数ペインでClaude Codeなどを同時表示した際の文字欠け・矩形化を修正する緊急リリース。

- Fix: 通常出力やリサイズのたびにWebGLの共有文字アトラスを消去していた処理を削除し、他ペインの既存文字が欠ける問題を解消。
- Perf: WebGL既定とコンテキスト消失時のDOMフォールバックは維持し、描画性能を落とさず安定性を回復。
- Test: 通常描画経路への `clearTextureAtlas()` 再混入を禁止する契約テストを追加。

## [0.14.3] - 2026-07-13

Online セーブポイントを「引き継ぎ記録」として再設計し、作業途中の更新と終了時の確定記録を分けたリリース。

- Feat: 「ここまでを保存」で更新できる作業途中の記録と、「終了時の記録を残す」で内容を上書きしない確定記録を追加。終了後に作業を続ける場合は、親の記録を保ったまま新しい世代を開始。
- UX: 画面名と操作名を「引き継ぎ記録」「作業途中」「終了時」「終了済み」「要点から続ける」「会話ごと続ける」に統一。確定操作の補足・ボタンを別段にし、折り返しと行間を調整。
- Fix: 確定処理の再実行、途中停止からの記録ID復旧、ピン・削除・期限整理の同期、旧 `Online` タブラベルの移行に対応。
- Fix: Dropbox Smart Sync の通常フォルダをリンクとして誤拒否せず、symlink / junction による保存先のすり替えだけを拒否。
- Test: TypeScript、画面、Rust、Pythonの回帰テストを拡充し、作業途中から終了時、再開後の新世代までを検証。

## [0.14.2] - 2026-07-13

ターミナル描画を WebGL 化する性能リリース。ストリーミング出力中に WebView2 renderer が1コアの40〜70%を常時消費していた問題への根治。

- Feat: xterm の描画を WebGL レンダラに変更 (既定ON)。context lost や初期化失敗時は、そのペインだけ自動で DOM レンダラに切り替わる (クラッシュ・自動再試行ループなし)。
- Feat: 外観設定に「ターミナルの描画方式」(WebGL/DOM) を追加。切替は開いているペインにも即時反映。過去に一部環境で発生した WebGL のテキスト破損が再発した場合の逃げ道。
- Test: レイアウト安定性の契約テストを新設計 (WebGL既定+フォールバック配線) に更新。

## [0.14.1] - 2026-07-13

設定ダイアログの体裁修正。タブ切替のたびにダイアログが伸縮して跳ねる問題を解消。

- Fix: 設定ダイアログの外形を固定 (920 × min(640px, 画面高−80px))。タブごとの中身の高さでダイアログが伸縮・再センタリングされる挙動を廃止し、中身側をスクロールに変更。
- Feat: 設定ナビをセクション化 (表示 / セッション / その他)。ナビ幅 208px 固定・ラベル1行固定 (省略記号)・hover 状態を追加。閉じるボタンを ✕ に変更し hover 反応を追加。

## [0.14.0] - 2026-07-13

設定画面を全面再編し、Online パネルとセッション復元まわりの改善をまとめた機能リリース。

- Feat: 設定をタブ付きダイアログに全面再編 (2026-07-12 UI監査の承認案)。外観 / 通知とレイアウト / Resume / リモート・共有 / アカウント・使用量 / Buddy / キーボードショートカット / アプリ情報 の8タブ。旧フラットメニューと ThemeSwitcher は廃止し吸収。トークン再生成は「危険な操作」として視覚的に分離。ラベルを日本語に統一 (背景パネル含む)。
- Feat: Online — セーブポイントを設定から独立させ、タイトルバーのアイコンに昇格。
- Feat: Online パネルの「このPCのセッション」を折りたたみフッター化。実行中/入力待ちの件数バッジ、idle 表示、行クリック (Enter/Space 可) で該当ペインへジャンプ。
- Feat: セーブポイントの手動削除と、transcript 不在時のわかりやすい公開エラー。
- Fix: セッション復元の自己修復 — ペイン再マウント時の偽ダウングレード警告を根絶 (PTY 生存中は検証スキップ)。完全ダウングレード時は無効化した保存IDを自動クリア (入力待ち中は絶対にクリアしない)、退避IDで復元できた場合は保存IDをそちらへ付け替え。
- Fix: 復元時に退避IDがあれば --continue へ落とす前にそれを resume。公開時は古いセッションIDでも cwd の最新 transcript へフォールバック。
- Fix: OAuth token リクエストに mycmux UA を送信 (429 の真因対応)。usage バーはメインアカウントのみ表示。
- Fix: MSYS で孤児化した agent プロセスの検出を維持。SHELL の usr\bin\bash.exe を Git bin ラッパーに書き換え。

## [0.13.5] - 2026-07-12

v0.13.4 配信後の実機再確認で残った、入力待ち中のしおり消失と会話復元の不透明さ・重複ID消去を修正。

- Fix: Claude が承認・入力待ちの間は agent session marker のクリアを抑止し、8秒確認 streak もリセット。Rust monitor がアイドル中の Claude を一時的に見失っても、しおりボタンと次回復元IDを保持する。
- Feat: 非アクティブかつ未マウントの保存済み会話タブに「再開待ち」サインを表示。通常タブと All tabs メニューの両方から、マウス・Enter・Space で再開できる。
- Fix: 同一会話IDが複数タブへ付いた場合、既存 dedupe 安全弁を維持したまま winner/loser を警告トーストで可視化し、敗者IDを最大5件の退避履歴として保存。退避IDは自動復元対象にはしない。
- Fix: live metadata の重複 claim は既存 owner を優先して拒否し、metadata fallback からの再混入も防止。タブ切替時の pane mirror を active tab と厳密同期し、旧タブIDの再注入を防ぐ。
- Note: OAuth アカウント追加の HTTP 429 は Anthropic/Cloudflare 側の制限でコード回避不可。v0.13.4 の診断ログを維持し、別回線1回または24時間以上停止後1回の切り分け結果待ち。

## [0.13.4] - 2026-07-12

v0.13.3 実機フィードバックの3症状 (OAuth 429 案内 / セーブポイントUI出没 / セッション復元不安定) を根本修正。UI 総点検の修正17件も同梱。

- Fix: しおり(公開)ボタンがエージェントのコマンド実行中に消えたり出たりする問題を修正 (processIsShell 単独の非表示ゲートを撤廃。公開は jsonl を読むだけなのでシェル前面でも実行可能)。
- Fix: Online パネルの「このPCで開いているセッション」で、再起動直後の復元候補 (未起動行) が約5秒で消える問題を修正 (dormant 判定を「metadata 不在」から「persisted マーカー基準」に変更)。
- Fix: プロセス検知の1回の誤観測で保存済みセッションID (復元情報) が恒久消失する問題を修正 — マーカーのクリアは「素のシェル状態が2ポーリング周期 (約10秒) 継続」で確定した場合のみ (App/SocketListener 共有ガード。pty_work_done 経由の即時クリアも廃止)。
- Fix: 同一フォルダで複数 Claude ペインを開いた際にセッションIDがペイン間で入れ替わる既知の cross-contamination を修正 (前 tick のヒューリスティック検出結果を tick 開始時に予約。explicit --session-id は従来どおり優先)。
- Feat: セッション復元がファイル不在で「直近の会話 (--continue)」へ無通知降格していたのを警告トーストで可視化。
- Fix: OAuth 429 の案内文言を実測に合わせて更新 (解除まで数時間〜1日以上の場合あり・再試行の繰り返しで制限延長の可能性)。カウントダウンは「最短目安」表記に変更。
- Feat: OAuth 失敗を `%APPDATA%/com.miyazaki.mycmux/logs/oauth.log` に永続記録 (HTTP status / cf-ray / retry-after / レスポンス先頭付き・token は記録しない)。エラー表示にも同診断情報を含める。
- Chore: usage API の User-Agent を claude-code/2.1.207 に更新。
- Fix: UI 総点検 (multi-agent audit) による可視性・レイアウト堅牢性・安定性の修正17件 — テーマ全色のコントラスト 4.5:1 をテストで固定、メニュー位置計算の共通化ほか。

## [0.13.3] - 2026-07-12

Online パネルのローカルセッション欄を強化し、Usage アカウント追加のレート制限画面を先に進める形に改善。

- Feat: 「このPCで開いているClaude Codeセッション」に未起動ペイン(復元候補)も「未起動」バッジ付きで列挙。会話履歴があれば未起動のままセーブポイントを保存できる。
- Feat: 各セッション行に稼働状態ドット(実行中/入力待ち)・ワークスペース名・gitブランチ・最終公開からの経過(未公開表示あり)を追加。
- Fix: セッション行のタイトルに「node.exe」等のプロセス名が出る問題を修正(フォルダ名ベースの命名に変更)。
- Style: 既存のダークトーンを維持したままパネルを高級化 — セクションのグラデーション枠・セッション数チップ・公開済み行の左アクセントレール・行/カードのホバー浮遊(reduced-motion対応)・「このPCで開いているセッション」バッジの発光。
- Fix: Usageアカウント追加の429エラーで先に進めなくなる問題を解消。「+ アカウントを追加」をエラー中も常時表示し、429/期限切れ時は「認証をやり直す」で新しい認証フローを開始。解除目安の30分カウントダウンをダイアログ再オープン後も継続表示。

## [0.13.2] - 2026-07-11

オンラインセーブポイントの保存導線とペインタブの溢れ挙動を、実機フィードバックに基づき全面改善。

- Fix: しおり(公開)ボタンがランチャー経由で起動した Claude Code タブに表示されない問題を修正。プロセス検出の時点でボタンを出し、セッションID確定までは理由つきの無効表示にする(agentId 依存を廃止・素のシェルには出さない)。
- Feat: ペイン内タブの溢れ設計を追加。タブは64pxまで縮小(アクティブは120pxを維持)し、溢れたら横スクロール(縦ホイール変換・アクティブタブ自動追従)+「All tabs」メニューで全タブへ到達可能。右側の操作ボタン群は縮小の影響を受けない。
- Feat: Online パネル上部に「このPCで開いている Claude Code セッション」一覧と保存/上書き更新ボタンを追加。しおりボタンと同じ公開動作をパネルからも実行できる。
- Feat: 公開中の進捗を4ステップ(履歴要約→引き継ぎ書生成→バンドル作成→登録)のチェックリストで表示し、完了時に新規/更新の別・保存先・パス警告件数を明示。

## [0.13.1] - 2026-07-11

オンラインセーブポイントのGUI導線を配信版へ収録し、複数ペイン運用で崩れていたセッション復元を修復。

- Fix: 保存済みセッションの実ファイルを全Claudeプロジェクトから検索し、重複時は最新の正本を選んで本来のcwdへ移動してから正確なIDをresumeする。空白・ドット・日本語を含むcwdも現行Claudeの保存先キーで照合し、別会話の`--continue`へ落ちる問題を解消。
- Fix: 同一cwdの複数ペインが同じ最新JSONLを取得しないよう、プロセス引数で確定したIDと保存済み所有者を優先。エージェント終了後の一時mappingも除去する。
- Fix: Online完全再開の`--fork-session`で、fork元IDではなく新しく生成されたセッションIDを追跡する。
- Fix: Online／ブラウザペインを表示したまま終了しても、次回起動時は最後に選択していた保存対象の端末へ戻る。
- Feat: 各Claude Codeペインのタブバーからオンラインセーブポイントを公開・更新できるGUIを追加。Onlineパネルにはローカルで開いているセッションを表示。
- Fix: Usageアカウント追加時のOAuth token exchange 429を、原因と再試行方法が分かる案内へ改善。

## [0.12.0] - 2026-07-10

複数の Claude Pro/Max アカウントの使用量を、タイトルバーで一元監視する機能を追加。

- Feat: **マルチアカウント usage 監視**。既存の単一アカウント版 UsageMeter を拡張し、追加した各アカウントの 5 時間・週次・Opus/Sonnet 消費率とリセット時刻をタイトルバーに worst 順のチップで表示。詳細はホバーのポップオーバーとアカウントごとのセクションで確認できる。
- Feat: **mycmux 自前 OAuth (PKCE) ログイン**。設定 → 「Usage アカウント管理」からブラウザ認証でアカウントを追加。refresh token は Windows Credential Manager に暗号化保存し、access token の期限が近づくと自動でリフレッシュする。取得スコープは `user:profile` のみ (read-only・推論 API には一切触れない)。
- Feat: **要再認証バッジと再認証フロー**。refresh token が失効したアカウントは警告表示になり、再認証ボタンから同じフローで復帰できる (アカウントの重複は作らない)。
- Feat: アカウントの有効/無効トグルと削除。usage 取得はアカウントあたり 180 秒間隔・低並列・429 時は静かにバックオフする。
- Note: 使用量取得は Anthropic の非公式エンドポイントを利用します。複数アカウントの追加・自動取得により、規約上のリスク (アカウント制限・停止) がゼロではない旨を設定画面に明記しています。

## [0.11.0] - 2026-07-10

品質集中パック (feat/terminal-display-stability) と resume 改善 (resume-phase-bc) の並列開発2ブランチを統合。

- Feat: **TUI 描画の安定化**。ConPTY を windowsPty 設定 + Unicode 11 幅計算で構成し、全角・絵文字を含む TUI (Claude Code 等) の描画崩れを低減。
- Feat: **xterm.js を 6.0.0 に更新** (DECSET 2026 synchronized output 対応)。エージェントの高速出力時のちらつきを抑制。
- Feat: **ウィンドウ位置・サイズ・最大化状態を再起動をまたいで復元**。
- Feat: **閉じたペインをセッションごと再オープン** (resume Phase C)。誤ってペインを閉じても直前のエージェントセッションに戻れる。
- Feat: **パレット resume の高速化 + launcher にセッションピッカー追加** (resume Phase C)。crsm 連携で過去セッションを一覧から選んで resume。
- Feat: **セッション検出とlauncher トラッキングの堅牢化** (resume Phase B)。session id 衝突時の再採番・resume 系フラグの検出を厳密化。
- Feat: launcher に `launcher.local.sh` / `launcher.local.ps1` ユーザー拡張フックを追加 (v3 R-3)。アプリ更新で上書きされないカスタマイズ層。
- Feat: 孤児セッションレコードを起動時に trash へアーカイブする retention 機構を新設。
- Feat: クリティカルな IPC 失敗・terminal cache 書き込み失敗をトーストで可視化 (v3 M-3)。
- Fix: AutoConsume がスクロールバックを破棄した際にフロントへ resync を通知し、表示と実体のずれを解消。
- Refactor: 重複していた ErrorBoundary を `common/ErrorBoundary` に統合 (v3 Q-1)。

## [0.10.1] - 2026-07-08

- Feat: **二重インストール検出** (v3 R-2)。起動時に `%ProgramFiles%\mycmux\` と `%LOCALAPPDATA%\mycmux\` の両方に exe が存在する場合、非ブロッキングの警告ダイアログを表示。0.9.1 で起きた「per-machine MSI が per-user 側の自動更新を止める」事故の実行時安全網。

## [0.10.0] - 2026-07-08

改善プラン v3 の P1〜P3 一括対応 (メモリリーク根治 / リモートのセキュリティ既定強化 / リリース安全網 / 体感性能)。

- Fix: **アクティブタブを閉じたとき xterm Terminal が dispose されず永久にメモリ滞留するリークを根治** (FE-N1)。タブを開いては閉じる agent 運用でメモリが線形増加していた「長く使うと重くなる」の主因。evicted マーク方式で close 時に確実に dispose し、リグレッションテスト5件を新設。v3 R-4 (dispose 順序の理論窓) も同時解消。
- Fix: socket ブリッジのコマンド待ちに 30 秒タイムアウトを追加 (RS-1)。フロント無応答時に tokio タスクと pending エントリが永久リークする穴を閉鎖。
- Security: **リモートサーバーの既定 bind を `127.0.0.1` (ローカルのみ) に変更** (v3 S-2)。従来どおり LAN/Tailscale から接続する (iPhone リモート等) には、設定 → Remote Access の「リモートを LAN に公開する (0.0.0.0 で待受)」を ON にして再起動してください (設定は data.json に永続化)。併せて終了時に全リモートシェルを kill する `kill_all` を quit_app / ウィンドウ破棄の両経路に配線し、`ip`/`tailscale` 呼び出しに 2 秒タイムアウトを追加。
- Perf: git ブランチ検出を monitor スレッドから 4 本のワーカースレッドに分離 (RS-5)。低速ドライブ×複数セッションで全セッションのメタデータ更新が「セッション数×最大2秒」直列ブロックしていた問題を解消 (ハング時の child.kill 保証は不変)。
- Perf: パスリンク解決にルート実在の事前チェックを追加 (FE-N2)。架空のパス風文字列 (最頻ケース) の stat 回数を最大 128 回 → 1 回に削減。
- Fix: `killSession` 失敗の黙殺 4 箇所を `console.warn` 化 (FE-N4)。
- CI: リリース workflow に test job を新設 (tsc / vitest / cargo test / pytest) し、build は test 通過後のみ実行 (v3 S-6)。updater feed 正規化を `scripts/normalize-updater-feed.ps1` に共通化 (v3 S-3、lite への配線手順は docs/DEPLOY.md 参照)。mirror の secret 未設定スキップを赤色表示化。
- Test: updater feed 契約テスト (plain キー == nsis エントリ) とバージョン一貫性テスト (5 ファイル一致) を新設 (v3 S-4/S-5)。7 リリース凍結していた package-lock.json の version を是正。

## [0.9.5] - 2026-07-08

- Feat: **分割・ズームボタンを狭いペインでも物理限界まで表示**。非表示になる閾値をペイン幅 300px→170px (分割)、240px→140px (ズーム) に緩和。列を増やしたときに Split ボタンが早々に消えて「それ以上分割できない」状態になる問題の解消 (右側ボタン群の実幅 ≈120px が収まる限り表示)。
- Feat: **Split right ボタン (右に分割) も設定でオンオフ可能に**。既定はオン (従来どおり)。設定メニューの「Split right ボタン（右に分割）を表示」で切替。Split down トグルと対称の設計。
- Security: リモート `/qr` エンドポイントに `/api/state` と同じ `?token=` 検証を追加 (未検証は 401)。同一 LAN / Tailscale 上の任意ホストが QR SVG 経由で平文トークンを取得できた穴を閉鎖 (改善プラン v3 S-1)。アプリ内の QR 表示は Tauri IPC (`get_remote_info`) 経由のため影響なし。

## [0.9.4] - 2026-07-08

- Feat: **フォルダパスもクリックで開けるように**。末尾が `\`/`/` のフォルダパスはクリック1回で Explorer がそのフォルダを開きます (日本語・スペース入り対応)。
- Feat: **実在チェックベースのパス解決**。拡張子も末尾セパレータもないパス (LLM 出力にありがちな「`C:\...\3_一次原稿` に置きました」形式) は、リンク検出時にディスクを確認して「実在する最長プレフィックス」だけをリンク化。実在フォルダはワンクリックで開き、実在しない文字列はリンク化しない (誤爆を構造的に排除)。新設 `resolve_local_path_links` command はホバー計算ごとに1回のバッチ IPC + キャッシュで軽量。

## [0.9.3] - 2026-07-08

- Feat: ターミナル出力中の**任意の拡張子のファイルパス** (`C:\...`・`/c/...`・`file:///...`) をリンク化。md/html/Office 以外のパスをクリックすると、クリック位置に2アイコンのポップアップ (「既定のアプリで開く」「エクスプローラーで表示」) を表示し、2タップで開けます。エクスプローラー表示は新設の `reveal_path_in_explorer` command (`explorer /select`・既存の URI 正規化を再利用)。md/html/Office は従来どおりワンクリックでアプリ内プレビュー。ポップアップは外側クリック / Esc / 実行で閉じます。

## [0.9.2] - 2026-07-08

- Feat: ペインタブバー右上の「Split down」ボタン (行を増やす方向の分割) を設定でオンオフ可能にし、既定を非表示に変更。列分割が主用途で行分割ボタンは押し間違えの原因になっていたため。設定メニューの「Split down ボタン（下に分割）を表示」で再表示できます (キーバインド等の分割機能自体は従来どおり)。
- Feat: launcher の「Change directory」を dev / anken の2メニューに分離し、Esc で前のメニューへ戻れるように。anken メニューを開いた際に起動ルート一覧をバックグラウンド更新。
- Change: launcher をキーボード専用に変更 (ホイール・クリック操作を廃止)。
- Fix: pty monitor 内でハングした git 子プロセスを kill するように (ペイン状態表示の固まり対策)。
- Chore: 死にコード削除 (forced updater loop / WebGL setting / storage::save)、clippy 警告解消。

## [0.9.1] - 2026-07-06

- Fix: 狭いペインでタブバー右端の × (Close pane) が最初に見切れる問題を修正。ステータス表示 (≤420px) → 分割ボタン (≤300px) → ズーム (≤240px) の順に畳み、+ と × は最後まで残します (container query)。
- Feat: バンドル launcher.sh に d キー / 「13. Change directory...」(`~/.mycmux/launch-roots.txt` からの起動ルート選択) とホイールナビゲーション (`__read_menu_event`) を焼き込み。起動時の launcher 再配布でユーザー編集が素の版に巻き戻る問題への恒久対応。
- Fix: personal updater feed の既定キー `windows-x86_64` を NSIS installer に向けるよう mirror script を修正。bundle marker なし (UNK) の生コピー exe が MSI を掴んで Program Files に per-machine インストールされ、稼働側が更新されない「二重インストール分裂」の根治。

## [0.9.0] - 2026-07-03

安定化リファクタ v2 全フェーズ実施 (`docs/plans/2026-07-03-stability-refactor-plan-v2.md`、6次元並列レビュー 24件確定に基づく Phase A/D/B/C/E)。

- Fix: ワークスペース切替時に常に先頭ペインへフォーカスが飛ぶ問題を修正。ワークスペースごとに最後のアクティブペインを記憶し復元します。
- Fix: WebView が 5 秒以上停止した後、ペインの生出力が remount まで黙って破棄され続ける「ACK 黒穴」を自己修復化 (matching-generation ACK / set_visible(true) で stale 状態を解除、backend 32KB scrollback replay で欠落分を復元)。
- Fix: Buddy 無効化トグルを backend に配線。ウィジェット非表示だけでなくセンサーパイプラインも実際に停止します。
- Fix: pinned roots の lost-update 競合を解消 (full snapshot 保存を所有権スコープの merge に変更し、pinned_roots はディスク値を保持)。
- Fix: 0 サイズに潰れたペインの pendingBatches 無制限成長に 2MB byte cap (oldest-drop + backend ring からの dup-guard 付き resync)。
- Fix: ドラッグ選択でペイン活性化を中断した際、DOM フォーカスを能動ペインへ復帰 (次のキー入力が非活性ペインへ吸われる問題)。
- Refactor: focus/activation を focusController に単一権威化。activePaneId の直接書き込み 20 サイトを 0 に集約し、focusin ゲート・リトライループ・wheel 抑止窓・タブクリック抑止タイマの競合 4 機構を明示的状態機械に吸収 (whack-a-mole 終結)。
- Refactor: commands/terminal.rs (4,661 行) を terminal / artifact / shell / session_mapping に純移動分割。XTermWrapper.tsx のモジュールヘルパ 5 本 (cache/focus/selection/mouse-filter/link-provider) を抽出。
- Perf: メタデータ購読のスコープ化 (ストリーミング中 ~150ms 毎の全アプリ再レンダ嵐を解消)、launch props のメモ化、可視性判定のフレーム内キャッシュ (毎バッチ強制 reflow 排除)、link provider LRU、monitor の negative TTL、broadcast clone の receiver ガード。
- Perf: AutoConsume メトリクス追加 (「ペインが固まった」報告の現地診断用)。
- Test: 挙動テスト基盤新設 — attachEpoch pure module + vitest (14 tests)、FrontendFlow の #[tokio::test] (backpressure コア初のユニットテスト)、focusController 状態機械テスト。
- Chore: 死に Tauri command 7 本削除、consumer_id 配管削除、debug 級ログ 20 箇所を dev ガード化 (error 級 31 箇所は常時出力を維持)、tao 0.34.6 → 0.34.8、paneMetadataStoreCompat → paneMetadataStore rename。
- Verification: npx tsc --noEmit / vitest 14 passed / cargo test 86 passed / python -m pytest tests -q 52 passed。

## [0.8.54] - 2026-07-02

Phase 1 安定化パック (2026-07-02 アーキレビュー計画 `docs/plans/2026-07-02-code-review-stability-perf-plan.md` に基づく)。

- Fix: data.json の置換を crash-safe 化。backup を copy 方式に変更して「data.json 不在の瞬間」を排除し、欠落・空・破損時は最新 pre-replace backup から自動復旧するようにしました。
- Fix: attach epoch を backend attach 成功後に確定するよう二相化。再アタッチ中・失敗時に旧 channel の生きた出力が stale 破棄されて表示が欠ける問題を修正しました。
- Fix: 終了時の最終保存が失敗した場合は quit せず、Retry / Quit anyway のダイアログを表示するようにしました。
- Fix: agent session mapping の保存前読み取りを TTL cache バイパス化し、mapping ファイルを atomic write + cache invalidation に変更 (セッション復元の session id 取り逃がし対策)。
- Fix: ブロッキング処理を含む同期 Tauri command 26 本を `#[tauri::command(async)]` 化し、UI スレッドハング (AppHangB1) の残存リスクを解消しました。
- Test: 同期 command の allowlist 契約テストを新設。新規 command が同期のままブロッキング処理を持つと CI で FAIL します (再発防止の本命)。
- Chore: 死にコード削除 (`get_all_cwds`、未登録の native browser backend 一式)、hot-path ログの DEV ガード、`VITE_UI_VARIANT=mycmux` 正式対応 (`cmux` は legacy alias)。
- Verification: cargo test --release 80 passed / python -m pytest tests -q 52 passed / npx tsc --noEmit / cargo check --release すべて成功。

## [0.8.53] - 2026-06-30

- Fix: Restore copy-on-select after terminal selection end, including context-menu word selection, cached terminal remounts, empty-selection focus restore, and clipboard fallback focus recovery.
- Fix: Make artifact preview opens visible and deterministic by activating reloaded preview tabs, suppressing duplicate opens while pending, showing short UI errors, and falling back to the OS opener when in-app preview fails.
- Verification: python -m pytest tests/test_layout_stability_contract.py tests/test_no_restart_ui_surface_contract.py -q / npm run build / cargo test --manifest-path src-tauri\Cargo.toml --lib -- --nocapture

## [0.8.52] - 2026-06-30

- Fix: Preserve Codex session identity on active restored tabs so startup restore resumes the saved Codex conversation instead of reopening the launcher.
- Fix: Store detected agent session mappings from the PTY monitor and PowerShell launcher, including Codex resume/handoff paths.
- Fix: Refresh terminal layout after pane resize, split, and visibility changes without coupling input startup to backend resize completion.
- Verification: python pytest restore/layout contracts, TypeScript check, frontend build, and Rust unit tests passed.

## [0.8.49] - 2026-06-24
- 修正: ペインタブ名のダブルクリック編集で、後続 click が通常のタブ選択に戻って入力欄を閉じる問題を防止しました。
- 検証: uv run pytest tests\test_layout_stability_contract.py tests\test_session_restore_race.py -q / npm.cmd run build

## [0.8.48] - 2026-06-24

- Fix: Keep pane tab rename editable on double-click by suppressing tab select and drag handoff before terminal focus can steal the rename input.
- Verification: npm.cmd run build, uv run pytest tests\test_layout_stability_contract.py tests\test_session_restore_race.py -q, and npm.cmd run tauri build passed.

## [0.8.47] - 2026-06-24

- Fix: Restore Claude Code / Codex wheel scrollback while preserving text selection. Terminal mouse-mode output is stripped from xterm, non-wheel terminal mouse input is blocked from the PTY, and wheel events are converted back to SGR mouse reports for agent TUIs so their in-app conversation history can scroll normally. DECSET alternate scroll mode (?1007) is also stripped from terminal output.
- Verification: cmd /c npx.cmd tsc --noEmit, py -3.12 -m pytest tests\test_layout_stability_contract.py tests\test_session_restore_race.py -q, cmd /c npm.cmd run tauri build passed. CDP probe could not connect to port 9223 on the restarted local app.

## [0.8.46] - 2026-06-23

- Fix: Restore normal text selection in Claude Code / Codex panes by stripping terminal mouse-mode control sequences before xterm processes output, including chunk-split sequences.
- Fix: Keep inactive-pane right-click word selection working without switching the active input pane; the CDP probe now accepts xterm's selection layer instead of only browser window selection.
- Verification: cmd /c npx.cmd tsc --noEmit, py -3.12 -m pytest tests\test_layout_stability_contract.py tests\test_session_restore_race.py -q, cmd /c npm.cmd run tauri build, and CDP focus/selection probe passed.
## [0.8.45] - 2026-06-23

- Fix: Preserve right-click terminal selection in Claude Code / Codex panes by stopping pane-level mousedown propagation while xterm mouse tracking is active.
- Change: Stop starting the forced auto-update loop from App.tsx; updates stay explicit instead of auto-relaunching after startup.
- Verification: python -m pytest tests\test_layout_stability_contract.py tests\test_session_restore_race.py -q, cmd /c npx.cmd tsc --noEmit, and npm.cmd run build passed.

## [0.8.35] - 2026-06-20

- Fix: Route terminal wheel events through xterm's custom wheel handler so wheel scrolling cannot be converted into CLI up/down input when the terminal buffer is not scrollable.
- Fix: Keep terminal focus synced to the session that owns the xterm element so typing, Backspace, Delete, and arrow keys keep going to the intended pane after mouse selection or pane switching.
- Fix: Restore terminal focus immediately after drag selection before clipboard writes finish, preventing temporary dead input after selecting text.
- Verification: npm.cmd run build and git diff --check passed.

## [0.8.34] - 2026-06-20

- Fix: Launch Menu now works on Windows PCs that do not have Git Bash by installing and running a PowerShell launcher.
- Fix: The launcher scripts are installed per user under `.mycmux/bin`, so GitHub-installed builds no longer rely on Miyazaki-specific shell setup.
- Fix: Missing commands now show a message instead of leaving a blank launcher pane.

## [0.8.33] - 2026-06-19

- Added forced automatic updates after startup, plus periodic, online, and visibility-resume checks. When a signed update is available, mycmux downloads it and relaunches without relying on the Settings button.
- Fixed terminal wheel scrolling by handling wheel events as local terminal scrollback and stripping mouse-report escape sequences before they reach the PTY.
- Added a release mirror script and workflow step for the personal updater feed so `mycmux-personal-updater/latest.json` can be kept on the current signed personal build.
- Verification: npm.cmd run build passed.

## [0.8.30] - 2026-06-19

- Fix: Restore normal terminal drag selection by removing the DOM-level terminal mouse guard. Mouse report escape sequences are filtered before they reach the PTY instead.
- Fix: Keep the blue pane border tied to the session that actually receives input or the selected tab. Selecting text in another pane no longer moves the input indicator.
- Verification: npm.cmd run build and git diff --check passed.
## [0.8.29] - 2026-06-19

- 修正: terminal 上の通常クリック・タップを CLI へ座標入力として送らず、focus 復帰だけに限定しました。カーソル移動は左右キーなどのキーボード操作に寄せています。
- 修正: xterm の Alt-click カーソル移動を無効化し、cached terminal の再利用後も同じ設定を維持します。
- 修正: 入力文字・Backspace・Delete・左右キーなどの PTY 書き込みを session ごとに直列化し、短い連続操作で順序が前後する余地を減らしました。
- 修正: 表示切替中の一瞬の非表示判定で入力を捨てないようにし、tab 切替・pane 操作・zoom 後の入力停止を防ぎます。
- 修正: terminal textarea がまだ生成されていない時に pane 本体へ focus して終了せず、実入力 textarea が取れるまで短く再試行します。
- 修正: IME composition 中は terminal shortcut 処理を介入させず、IME 入力を xterm にそのまま渡します。
- 検証: `cmd /c npm run build` を通過しました。

## [0.8.28] - 2026-06-19

- 修正: pane の発光表示が DOM focus に反応して、マウス移動や意図しない focus 移動で光る pane が変わる問題を直しました。
- 修正: active pane の発光は pointer down、つまりクリック・タップで入力先を選んだタイミングに合わせて更新します。
- 修正: 別 pane をクリックした直後に、前の pane の blur が新しい active pane を消してしまう経路を防ぎました。
- 検証: TypeScript 型チェックと layout 安定化契約テスト 13 件を通過しました。

## [0.8.27] - 2026-06-19

- 修正: Resume 実行前の CRSM session 検証で毎回 `--refresh` を要求していたため、CRSM の再走査が 6 秒を超えると `CRSM session validation timed out after 6000 ms` で pane が開かない問題を直しました。
- 修正: Resume 直前の CRSM 照合は軽い ID 検索に変更し、照合が遅い・失敗する・選択済み session が再検索結果に出ない場合でも、一覧で選んだ session ID を使って起動します。
- 維持: 実体ファイルがない Claude 要約履歴は、これまで通り直接 Resume せず、別履歴または引き継ぎを案内します。
- 検証: 手元の CRSM 実測で `--refresh --query <session-id>` が約 6.9 秒、`--query <session-id>` が約 0.16 秒になることを確認し、Resume 前の重い再走査を外しました。

## [0.8.26] - 2026-06-19

- 修正: cached terminal を再利用したあとも `onData` / `onBinary` / `onTitleChange` を必ず登録し直し、pane 移動・tab 切替・復元後に CLI へ入力が届かなくなる経路を塞ぎました。
- 修正: 非表示 pane への入力送信を止め、実際に入力した terminal の session を active pane に戻すようにしました。閉じた pane の session ID が active に残って Backspace や文字入力が効かなくなる状態を防ぎます。
- 修正: xterm 上の通常文字、Backspace、Delete、矢印などは global shortcut より CLI 入力を優先します。壊れたショートカット設定が `h` などの通常入力を奪う事故を防ぎます。
- 修正: pane close、split、tab 追加、通知パネル、drag-and-drop 後の focus 復帰先を xterm の実入力 textarea に限定し、描画直後でまだ要素がない場合は短く再試行します。
- 修正: browser preview pane を保存時に空 terminal pane として残さないようにし、復元後に「消したはずの pane が出る」「別 pane が出る」状態を抑えました。
- 修正: 横分割で新しい列だけ幅 `1` になり、pane が極端に細くなる・横スクロールが出る問題を修正しました。列構成が変わった場合は一度等分幅へ戻します。
- 検証: TypeScript 型チェックと layout/input 安定化契約テスト 13 件を通過しました。

## [0.8.25] - 2026-06-18

- 修正: 端末で文字を選択して自動コピーしたあと、入力フォーカスが一時的なコピー用 textarea に残って CLI へ文字入力や Backspace が届かなくなる問題を直しました。コピー成功時・失敗時・fallback コピー時のすべてで xterm へフォーカスを戻します。
- 修正: タブ追加、タブ切替、タブ削除、pane 分割、pane close、zoom 操作のあと、ボタン側へフォーカスが残って CLI 入力できなくなる経路を直しました。操作後は対象 pane、または分割後に active になった pane の端末入力欄へ戻します。
- 修正: キーバインド設定が途中で読み込まれても、修飾キーなしの通常文字、Backspace、Delete、矢印キーなどは必ず CLI 入力として通すようにしました。壊れたショートカット設定で `h` などの通常入力が奪われる事故を防ぎます。
- 修正: 端末出力が大量に出て frontend queue が満杯になったとき、PTY reader が待ち続けて stdout/stderr が詰まり、結果として stdin まで固まったように見える経路を直しました。満杯時は表示用チャンクを捨ててでも PTY の読み取りを継続します。
- 検証: TypeScript 型チェック、layout 契約テスト、Rust unit test 75 件で、入力フォーカス復帰・通常入力保護・PTY reader 非ブロック化を確認しました。

## [0.8.24] - 2026-06-18

- 修正: 端末を pane 分割や復元で再利用したあとも、選択した文字列だけがコピーされるようにしました。選択中の中間状態を何度も clipboard に書き込む処理をやめ、選択完了時に一度だけコピーします。
- 修正: pane の列・行構造が変わったときに、古い分割 UI の内部状態が残って上下左右の関係が崩れる問題を抑えました。実際の `splitColumns` から layout key を作り直し、古い列 key の使い回しをやめています。
- 修正: 保存済みの幅・高さ情報が現在の pane 構造と合わない場合は破棄するようにしました。古い比率が別の pane に流用されて表示が欠ける状態を防ぎます。
- 修正: Resume パレットで選んだ履歴を起動する前に、最新の CRSM 履歴で同じ session が存在するか確認します。見つからない履歴や直接 Resume できない Claude 要約履歴は、新しい pane を作る前に案内するようにしました。
- 修正: 保存済み pane の復元で session 検証に失敗したとき、古い session ID が CLI 引数に残って `specified session not found` になる問題を直しました。
- 検証: 選択コピー、pane layout、Resume 起動前検証、古い Resume 引数の除去について回帰テストを追加しました。

## [0.8.23] - 2026-06-18

- 修正: pane を削除・移動・preview へ差し替えた時に、実際の pane 一覧と `splitColumns` がズレたまま残る問題を直しました。古い配置情報に残っていた pane が、次の削除や分割操作で突然出てくる状態を防ぎます。
- 修正: 呼び出し元が `splitColumns` を渡さない pane 更新でも、現在の pane 一覧に合わせて配置情報を必ず補正し、列構造が変わった場合は幅・高さの保存値も作り直すようにしました。
- 検証: レイアウト安定性テストを更新し、pane 一覧と配置情報が同期されることを確認しました。

## [0.8.22] - 2026-06-18

- 修正: pane 分割時に横スクロールで逃げず、画面幅いっぱいの範囲内で各 pane が細くなるようにしました。
- 修正: 保存済みの古い `columnWidths` / `rowHeightsPerCol` が画面幅を超えていても、現在の表示領域へ縮尺してから Allotment に渡すようにしました。
- 修正: active pane 追従用の `scrollIntoView` を廃止し、pane 追加時に下部や左右のスクロールバーが出る原因を取り除きました。

## [0.8.21] - 2026-06-18

- 修正: pane 数が増えたときに、列ごとの最小幅 `420px` が画面幅を超えて横スクロール前提になり、pane が画面内に収まらない問題を修正しました。列幅は表示領域に合わせて縮むようにし、狭い状態でも全 pane が画面内に残ります。
- 修正: 横分割後に新しい pane が active にならず、作った pane が見えない位置に残って「pane が開かない」ように見える問題を修正しました。分割直後は新 pane にフォーカスします。
- 修正: `splitColumns` と実際の pane 一覧がずれた場合に、pane は存在するのに描画対象から外れる問題を修正しました。保存・復元・表示の各入口で欠けた pane ID を補正します。
- 修正: active 状態が tab session ID を指しているとき、ショートカットの横分割・縦分割・zoom が対象 pane を見つけられない問題を修正しました。
- 改善: レイアウト安定性テストを更新し、固定幅スクロール前提ではなく、画面幅追従・pane 欠損補正・分割後フォーカスを契約として検証するようにしました。

## [0.8.20] - 2026-06-18

- 修正: ワークスペース復元時に、横分割を勝手に縦積みへ変換してしまう問題を修正しました。
- 修正: pane 操作、復元、保存のすべてで、保存済みの分割方向を保ったままレイアウトを整理するようにしました。
- 修正: stale な `column_widths` / `row_heights_per_col` は、現在の `split_columns` と形が合わない場合に使わないようにしました。
- 修正: 端末描画を WebGL から安定優先の DOM 描画へ戻し、透明背景・複数 pane 環境で文字が斑に欠ける症状を抑えました。
- 修正: 分割・リサイズ中に一時的に 0 サイズ扱いになった pane の出力を破棄せず、表示復帰後に描画するようにしました。
- 修正: mycmux の二重起動を防ぎ、複数プロセスが同じ `data.json` を競合保存してレイアウトを壊す経路を止めました。
- 修正: `data.json` の読み書きにもプロセス間ロックを追加し、万一の同時保存でも直列化されるようにしました。

## [0.8.19] - 2026-06-18

- 修正: 複数 pane のワークスペースが復元後に読めないほど細い列へ潰れる問題を修正しました。
- 修正: pane 分割構造が変わったとき、幅・高さの保存値を捨てっぱなしにせず、使える範囲で整合させるようにしました。
- 変更: 端末グリッドに最小列幅・最小行高を設定し、必要な場合は横スクロールで読める状態を保つようにしました。

## [0.8.18] - 2026-06-17

- 修正: personal 版の更新フィードを lite 版の `/releases/latest` から分離しました。
- 修正: personal 版は `mycmux-personal-updater` の安定した公開アセットを読むようにし、private release 由来の 404 と署名鍵の不一致を避けました。

## [0.8.17] - 2026-06-17

- 修正: 起動時復元で、同じ agent session ID が複数 pane に残る問題を修正しました。active tab を優先し、重複側の resume 情報を落とします。
- 修正: `session_id` ごとの PTY 作成を直列化し、復元・再接続経路が同じ session を二重 spawn しないようにしました。
- 修正: personal 版が team/lite の release feed を見に行かないようにし、設定画面の更新確認が正しい署名鍵とパッケージを使うようにしました。

## [0.8.16] - 2026-06-05

- 修正: zoom 中の pane を閉じたとき、または zoom 対象を含まない workspace に切り替えたときに画面が空になる問題を修正しました。
- 修正: stale な `zoomedPaneId` を store 側で消し、`AppShell` にも自己回復ガードを残しました。
- 修正: セッション数や出力量が増えたとき、PTY command 処理が UI thread を固める問題を改善しました。

## [0.8.6] - 2026-05-20

- 変更: buddy avatar の monitor/screen 枠を外し、以前のように buddy 領域へ直接描画するように戻しました。
- 修正: 表情 sprite が切り替え時に分裂したり揺れたりする問題を修正しました。表情ごとに要素を作り直さず、1つの永続要素の background-position だけを更新します。

## [0.8.5] - 2026-05-20

- 修正: 表情 avatar が数秒ごとに揺れたり、表情変更時に位置・サイズが跳ねたりする問題を修正しました。
- 修正: sprite atlas の各セルを個別に crop / scale していた処理をやめ、全セルへ同じ変換を使うことで表情間の位置合わせを保ちました。
- 修正: renderer 側の crossfade から layout projection 由来の揺れを取り除きました。

## [0.8.4] - 2026-05-20

- 追加: buddy avatar で face-expression skin を使えるようにしました。
- 追加: `layout.json` で定義された表情セル、mood group、blink、idle ambient、talking lip-sync を読み、buddy の気分と発話に応じて表情を選ぶ expression director を追加しました。
- 追加: skin 画像と layout は repo 外のローカル assets として扱い、標準ビルドは従来の SVG face を使います。

## [0.8.3] - 2026-05-19

- 追加: buddy が状況に合うときだけ、Steins;Gate の定番台詞をまれに発話へ織り込めるようにしました。
- 追加: 台詞は外部ライブラリから読み、同じ台詞の連続使用を避けます。
- 追加: `persona.steinsQuotes` の `enabled` / `chance` / `minGapMinutes` で調整できます。

## [0.8.2] - 2026-05-19

- 修正: buddy 初期化を起動直後の重い処理から外し、最初の Claude/Codex 活動または起動後 90 秒で遅延実行するようにしました。
- 修正: 入力中の idle 判定だけで buddy が話し出さないようにしました。Claude/Codex 活動と interval check のみで反応します。
- 修正: buddy の cross-session scan、session tail、environment scan を main thread から分離し、通常利用中の短い引っかかりを減らしました。
- 修正: daily summary と weekly report の observation-state 書き込みを直列化し、JSON 書き込みを atomic にしました。
- 修正: renderer の unhandled rejection と Rust panic をログへ出すようにしました。

## [0.8.0] - 2026-05-18

- 追加: embedded buddy を Amadeus persona として再構成しました。
- 追加: v8 Kurisu persona、長期記憶、週次自己観測、発話 cooldown、interval-driven initiative を導入しました。
- 追加: buddy core を adapter 層の後ろへ分離し、persona は casual register で話すようにしました。
- 追加: Codex-pet spritesheet を base64 data URL で読み込めるようにしました。

## [0.7.8] - 2026-05-17

- 修正: Codex など大量出力時の端末表示安定性を改善しました。
- 修正: frontend queue が詰まっているときに PTY 出力を捨てず、backpressure をかけるようにしました。
- 修正: session 再接続時に data channel を正しく差し替え、古い callback が表示へ書き込まないようにしました。
- 修正: resize 時に段階的な full refresh を行い、端末を再描画しやすくしました。
- 修正: WebGL context loss への復旧処理を強化しました。

## [0.7.7] - 2026-05-16

- 修正: theme picker で選んだ theme が実際に反映されるようにしました。
- 修正: light theme 選択時に ANSI palette が dark 前提のまま残る問題を修正しました。

## [0.7.6] - 2026-05-16

- 修正: 背景画像の上で terminal 文字色が薄くなりすぎる問題を修正しました。
- 変更: media background 有効時は xterm の `minimumContrastRatio` を無効化し、透明 terminal 背景に合う色を保ちます。

## [0.7.5] - 2026-05-16

- 変更: UI 色を theme token ベースへ整理しました。
- 変更: modal、command palette、status badge、drag-and-drop、chrome の hardcoded dark 前提色を theme-aware token へ置き換えました。
- 追加: backdrop、shadow、on-accent、on-status、spacing token を追加しました。
- 修正: yoi-ai theme の文字コントラストを WCAG AA 水準へ引き上げました。

## [0.7.4] - 2026-05-16

- 修正: title bar の文字・icon shadow を、theme の宣言ではなく実際の chrome 背景明度で決めるようにしました。

## [0.7.3] - 2026-05-16

- 変更: title bar の文字・icon shadow を theme-aware にしました。light theme では暗い drop-shadow を落とし、画像背景時だけ薄い white halo を残します。

## [0.7.2] - 2026-05-16

- 追加: pane tab の double-click / context menu rename に対応しました。label は `data.json` に保存され、空にすると自動命名へ戻ります。
- 追加: Resume palette で user message のない session を非表示にできる設定を追加しました。
- 変更: Usage Meter はローカル JSONL 推定ではなく Anthropic OAuth `/usage` endpoint の live 値を読むようにしました。
- 変更: ChatGPT rate-limit endpoint に到達できない場合は Codex usage section を自動で隠します。

## [0.7.1] - 2026-05-14

- 追加: PTY metrics、SessionManager create log、frontend attach epoch、xterm 1 秒統計、WebGL loss、termCache lifecycle、initial replay log を入れ、表示欠け・復元不具合の調査をしやすくしました。
- 修正: TitleBar の文字色 token を調整し、視認性を上げました。
- 修正: Claude usage 集計から `cache_read_input_tokens` を除外しました。
- 修正: usage tier の `max_20x` と Codex limit の初期値を実測寄りに校正しました。
- 備考: WebGL 表示のプツプツ、SessionManager channel 差し替え、scrollback/replay 重複表示は次段の調査対象として残しました。

## [0.7.0] - 2026-05-13

- 追加: TitleBar に Claude Code / Codex の Usage Meter を追加しました。5 時間ローリングと 7 日 window を常時表示します。
- 追加: Rust native の usage 集計モジュールと `get_usage_summary` Tauri command を追加しました。
- 追加: `~/.claude/projects/**/*.jsonl` と `~/.codex/sessions/**/*.jsonl` を直接集計し、差分スキャン用 file cache を使います。
- 修正: 同一 `session_id` の二重 `create_session` が既存 PTY を壊す問題を、`SessionManager::create` の idempotent 化で修正しました。
- 修正: cwd 検証経路と spawn 経路で参照パスがずれる問題を、`resolve_launch_cwd` の共有で修正しました。
- 修正: 並列復元 race を抑えるため、startup autosave hold、mapping refresh、初回 mount delay を調整しました。
- 変更: `scripts/backfill-sessions.ps1` を deprecated にしました。pane config に agent kind / cwd が保存されるため、起動後 backfill は不要です。

## [0.6.2] - 2026-05-07

- 修正: active workspace だけでなく、保存済み Claude / Codex / claude-codex session を持つ全 workspace を復元対象にしました。
- 変更: inactive workspace は active workspace の後に短い queue で mount し、通常起動の応答性を保ちながら復元します。
- 修正: `shell-starter` / session-less pane は pane-session mapping から `agent_kind` と `agent_session_id` を復元できます。
- 修正: workspace LRU mount cap は shell-only workspace のみに適用し、復元対象 workspace は evict しないようにしました。
- 修正: 起動復元中は autosave を短時間止め、中間状態の `data.json` で上書きされにくくしました。

## [0.6.1] - 2026-05-07

- 修正: session history persistence の実修正を入れました。
- 修正: `onPtyMetadata` が `paneMetadataStore` へだけ流れ、`SocketListener.tsx::toConfig` が参照する `Pane.claudeSessionId` / `agentKind` / `agentSessionId` に反映されない問題を修正しました。
- 注意: 既存 v0.6.0 の `data.json` は勝手に書き換えません。session id が `null` で保存されていた pane は一度 launcher menu に戻り、再起動後の次回保存から自動再接続できるようになります。
- 備考: env leak 防止の `stripEphemeralLaunchEnv`、`EPHEMERAL_LAUNCH_ENV_KEYS`、`remove_var`、`dedupeAgentSessionsInConfigs` は維持しました。

## [0.6.0] - 2026-05-05

- 追加: v0.5.x 系の修正をまとめて正式反映しました。
- 修正: session history persistence を戻し、再起動時に各 pane が以前の Claude / Codex session へ再接続できるようにしました。
- 追加: Settings に Terminal renderer (WebGL) toggle を追加しました。既定は macOS=ON、Windows=OFF、Linux=OFF です。
- 確認: launcher menu の 10 item 構成を normal → dangerous → resume の順に揃えました。

## [0.5.6] - 2026-05-05

- 修正: `SocketListener.tsx::toConfig` が live の `claudeSessionId` / `agentKind` / `agentSessionId` を `data.json` に保存するように戻しました。
- 修正: `data.json` と `~/.mycmux/pane-sessions/*.txt` mapping cache を使い、再起動後に各 pane が以前の Claude / Codex session へ戻れるようにしました。
- 注意: v0.4 で無効化していた理由は `MYCMUX_*` env leak でしたが、v0.4.x で防御済みのため再有効化しました。

## [0.5.5] - 2026-05-05

- 追加: Settings に Terminal renderer (WebGL) toggle を追加しました。
- 変更: 既定は macOS=ON、Windows=OFF、Linux=OFF です。
- 修正: Windows は v0.5.1 相当の DOM renderer を既定に戻し、WebView2 + WebGL の濃く重い描画を避けました。
- 注意: renderer の切り替えは既存 live pane ではなく、次に作る pane から反映されます。

## [0.5.4] - 2026-05-05

- 確認: `src-tauri/src/launcher.sh` の launcher menu を canonical な 10 item 構成へ揃えました。
- 変更: normal → dangerous → resume の順で表示します。
- 備考: source file は v0.4 以降この順でしたが、installer を更新して `~/.mycmux/bin/launcher.sh` も一致させました。

## [0.5.3] - 2026-05-05

- 修正: `terminal_config.rs` の alacritty loader で、非 Linux / 非 macOS target の unused variable lint を抑制しました。
- 修正: `commands/workspace.rs` の不要な trailing `return;` を削除しました。
- 修正: `db/storage.rs` の cleanup を `inspect_err` に置き換えました。
- 備考: v0.5.2 tag の Windows clippy 修正を、きれいな version として再配布した release です。

## [0.5.2] - 2026-05-05

- 修正: macOS で native window decoration を使い、`decorations: false` 由来の idle CPU 異常を回避しました。
- 追加: xterm WebGL addon を接続し、context loss 時は DOM fallback へ戻るようにしました。
- 変更: unused な `ghostty-web` dependency を削除しました。
- 追加: macOS build path を正式サポートしました。
- 変更: macOS の `Cmd+...` を Windows 由来の `Ctrl+...` shortcut と同等に扱います。
- 修正: `terminal_config.rs` の macOS / Linux build failure を修正しました。
- 修正: `crsm` CLI lookup は `.exe` 決め打ちをやめ、platform suffix を使うようにしました。
- 修正: macOS で window が出ない場合に `show()` を setup hook から呼ぶ bridge を追加しました。

## [0.5.1] - 2026-05-05

- 改善: Resume / CRSM Palette は cached session list から即表示し、背後で CRSM を更新するようにしました。
- 改善: 大量履歴でも重くならないよう、request dedupe、10 秒 auto-refresh cooldown、初回 1000 session load、明示 deep load を入れました。

## [0.5.0] - 2026-05-04

- 追加: Claude Buddy が Codex Pet skin を使えるようにしました。
- 追加: `~/.codex/pets/<name>/{pet.json,spritesheet.webp}` を selectable skin source として扱います。
- 追加: installed Codex Pets を取得する `list_codex_pets` Tauri command を追加しました。
- 追加: `~/.claude-buddy/config.json` を保存する `save_buddy_settings` Tauri command を追加しました。
- 追加: Settings menu に Buddy skin section を追加しました。
- 追加: buddy mood を idle / listening / thinking / tsukkomi / applaud / curious / amused / alert / sleepy へ対応付けました。

## [0.4.4] - 2026-05-04

- 修正: GitHub Actions の release workflow で、`workflow_dispatch` の tag input を渡しても release upload が skip される問題を修正しました。
- 変更: `tagName` / `releaseName` / `releaseBody` は `${{ github.event.inputs.tag || github.ref_name }}` を参照します。
- 変更: release workflow を repo 別の専用 job へ整理しました。master worktree は `build-personal` のみです。

## [0.4.3] - 2026-05-04

- 修正: 背景画像有効時、Settings menu や通知 dropdown に `panelOpacity` が乗って読みにくくなる問題を修正しました。
- 追加: popover 系だけを不透明にする `--cmux-popover` を導入しました。
- 変更: CRSM Palette を Resume にリブランドしました。内部 symbol と localStorage key は互換性維持のため変更していません。
- 変更: Settings 内の Resume 関連設定を1ブロックにまとめました。

## [0.4.2] - 2026-05-04

- 追加: 右上 Settings menu に `Themes` / `Keybindings` と並んで `CRSM Palette` button を追加しました。
- 変更: CRSM Palette で非表示にした kind は、handoff target button と Tab key cycle からも消えるようにしました。

## [0.4.1] - 2026-05-04

- 追加: Settings に `Claude Code` / `Codex` / `Hybrid` の表示 ON/OFF checkbox を追加しました。
- 追加: Remote QR button で現在 URL、SVG QR、token suffix、接続中 client 一覧を表示できるようにしました。
- 追加: token 再生成を Settings から実行できるようにしました。
- 変更: Remote URL は `#token=` から `?token=` へ移行し、WebSocket upgrade 時に server が直接 token を確認できるようにしました。
- 修正: `acda1ff` 由来の半完成 remote control 差分を完成形にし、HEAD 単独で `cargo check` が通るようにしました。

## [0.4.0] - 2026-05-03

- 修正: CRSM Palette 経由の env が親プロセスから他の PTY へ漏れ、新規 pane が意図せず resume される問題を修正しました。
- 修正: CRSM CLI 呼び出し時に Windows console window が一瞬出る問題を抑制しました。
- 追加: 詳細 subpanel、cwd filter chip、kind badge、相対時刻、さらに過去の session 読み込み、開始時刻表示を追加しました。
- 変更: CRSM Palette を 1200px 幅の 2 column 構造へ拡張しました。
- 変更: `agent_session_id` / `agent_kind` / `claude_session_id` を `data.json` に保存しない仕様へ変更しました。再起動後は Ctrl+P から手動 resume します。

## [0.3.8] - 2026-04-28

- 追加: team distribution を `miyafcos/mycmux-team` から公開し、in-app updater を public release feed へ向けました。
- 変更: README の install command を team repository 用に更新しました。
- 追加: UI または shortcut から workspace を閉じる前に確認を出すようにしました。
- 変更: launcher option を整理し、Claude auto-mode を外し、Claude / Codex / claude-codex の dangerous / resume entries を追加しました。

## [0.3.6] - 2026-04-26

- 修正: 復元された Claude / Codex tab が shell launcher へ戻らず、実際の agent id で再起動するようにしました。
- 修正: 復元 agent session では stale terminal snapshot replay を skip し、古い `Launch:` 画面が resume を隠さないようにしました。
- 修正: Windows `.cmd` / `.bat` shim を `cmd.exe /d /c` 経由で解決し、`claude.cmd` などの CLI shim を起動できるようにしました。

## [0.3.5] - 2026-04-26

- 修正: stale pane-session mapping が同じ Claude / Codex session を複数 pane に復元する問題を防ぎました。
- 修正: duplicate mapping を捨てる場合でも、shell launcher pane は shell pane として残すようにしました。

## [0.3.3] - 2026-04-24

- 追加: 背景・cache pane の Codex approval prompt と番号選択肢を検出し、Codex が許可待ちのときに通知できるようにしました。

## [0.3.2] - 2026-04-24

- 修正: Tauri updater artifact generation を有効化し、GitHub Releases に in-app updater が期待する signed metadata を含めるようにしました。

## [0.3.1] - 2026-04-24

- 修正: PTY から frontend への IPC path を session ごとの queue と rate-limited forwarder で制御し、WebView stall 時に Tauri Channel queue が無制限に増えないようにしました。
- 改善: PTY scrollback append path を batch extend / drain で効率化しました。
- 修正: local MSVC linker path の決め打ちを外し、GitHub Actions runner の Visual Studio toolchain を使えるようにしました。

## [0.3.0] - 2026-04-23

- 修正: hidden workspace / hidden tab が裏で動き続ける問題を止めました。
- 改善: workspace mount set を LRU 3件に制限し、pane は active tab だけ render します。
- 改善: `XTermWrapper` は cache / reattach 時に listener を正しく dispose / re-register します。
- 追加: `tauri-plugin-updater` v2 による in-app auto-update を追加しました。Settings → 更新を確認 から確認、署名検証、download、自動再起動まで行います。
- 変更: 個人版と lite で updater 署名鍵を分離しました。
- 追加: `build-personal.ps1` / `build-lite.ps1` を分離し、branch 確認、clean 確認、MSVC 読込、build、backup、配置を1コマンド化しました。
- 変更: GitHub Actions release workflow を Windows 専用に整理しました。
- 変更: tag 命名は personal が `vX.Y.Z`、lite が `vX.Y.Z-lite.N` です。

## [0.2.0] - 2026-04-07

- 追加: WebSocket ベースの browser access を持つ Remote Terminal dashboard を追加しました。
- 追加: iPhone / remote viewer 用の既存 session monitoring flow を追加しました。
- 追加: Berry Cream、Ocean Mist、Matcha Latte の3つの theme を同梱しました。
- 修正: Kitty style terminal input の `Shift+Enter` を扱えるようにしました。
- 改善: Windows deployment と local update script の metadata を揃えました。
- 備考: この release は当時利用中の local `mycmux` build と一致します。
- 備考: 一部の内部 document と legacy artifact には `ptrterminal` / `ptrcode` 表記が残っています。

## [0.1.3] - 2026-03-20

- 追加: browser pane の初期実装を入れました。
- 制約: 多くの実サイトは `X-Frame-Options` や `Content-Security-Policy` により iframe 表示できません。
- 制約: host からの JavaScript 実行は same-origin policy により制限されます。
- 制約: cross-origin navigation では back / forward が期待通り動かない場合があります。
- 追加: `browser_create`、`browser_destroy`、`browser_set_bounds`、`browser_navigate`、`browser_eval`、`browser_status`、`browser_snapshot` を追加しました。
- 追加: mount 時に container 座標で browser webview を作り、ResizeObserver で bounds を同期し、unmount で破棄します。
- 追加: URL bar は入力を正規化し、必要に応じて `https://` を補います。

## [0.1.2] - 2026-03-18

- 修正: browser pane 内で keydown を parent window へ relay し、browser focus 中でも global shortcut が動くようにしました。
- 修正: pane close 後、focus が失われず次の pane へ移るようにしました。
- 追加: browser agent API の初期 socket command を追加しました。

## [0.1.1] - 2026-03-17

- 修正: terminal pane を split しても shell session ID を reset しないようにし、duplicate session creation を防ぎました。

## [0.1.0] - 2026-03-15

- 追加: Tauri + xterm.js ベースの multi-workspace terminal multiplexer を作成しました。
- 追加: pane の horizontal / vertical split と drag resize を実装しました。
- 追加: workspace persistence、theme system、notification system、Unix socket API、custom title bar を追加しました。
