# ailog 便2 Front 完了

## 実装

- 段バーを `比較・学び・記録・日別` の4本へ再編し、既定を記録に変更。
- 記録へ旧「全体」と「内訳」を統合。トータル、日別積み上げ、要約カード、案件別、セッション一覧、補助 details を収容。既定ソートは手戻り順。
- 学びへ罠・決めごと・失敗コマンド・書き直しファイル・ルール通信簿を統合。比較からルール通信簿を除去。
- 日別に提案コピーと選択日の記録への導線を追加。
- 段ごとの `lastLoadMs` を記録し、dev ビルドのヘッダに表示。

## 旧6段から4段への対応

| 旧段 | 移行先 |
| --- | --- |
| 全体 | 記録: トータル・日別積み上げ・稼働リズム・モデル表 |
| 内訳 | 記録: 要約カード・案件別・セッション一覧・内訳の次元 |
| 比較 | 比較 |
| 日別まとめ | 日別 |
| 繰り返しの罠 | 学び: 罠・失敗コマンド・書き直しファイル |
| 決定事項 | 学び: 決めごと・ルール通信簿 |

## 検証

- `npx tsc --noEmit` : PASS
- `npx vitest run` : PASS (167 files, 2017 tests)
- `git diff --check` : PASS

## セルフレビュー

1. 構造レビュー: 段ID、ロード、初期段、クロス段導線、LLM明示ゲート、期間キャッシュとidle直列先読みを照合。
2. 境界レビュー: 変更は `src/components/ailog/**`、`src/stores/ailogStore.ts`、`tests/unit/**`、本ファイルのみ。Rust/APIは未変更。
